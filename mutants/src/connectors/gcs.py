"""
Conector Google Cloud Storage — Operaciones de Almacenamiento
================================================================

Permite subir, descargar, listar y gestionar objetos en
buckets de Google Cloud Storage usando la REST API v1.
"""

from __future__ import annotations

import base64
import contextlib
import json
import os
import time
from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁGcsConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_upload_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_upload_simple__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_download_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_list_objects__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_delete_object__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_list_buckets__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut: MutantDict = {}  # type: ignore


class GcsConnector(BaseConnector):
    """Conector para Google Cloud Storage: operaciones CRUD en buckets y objetos.

    Usa la REST API de GCS (storage.googleapis.com/storage/v1)
    con autenticacion OAuth2 Bearer token.
    """

    name = "gcs"
    version = "1.0.0"
    description = "Sube, descarga y gestiona archivos en buckets de Google Cloud Storage"
    category = "cloud_storage"
    icon = "cloud"
    author = "Zenic-Flijo"

    # URL base de la GCS JSON API
    _GCS_API_BASE = "https://storage.googleapis.com/storage/v1"
    _GCS_UPLOAD_BASE = "https://storage.googleapis.com/upload/storage/v1"

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._GCS_API_BASE
        self._http: HttpClient | None = None
        self._access_token: str = ""
        self._project_id: str = ""

    def xǁGcsConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._GCS_API_BASE
        self._http: HttpClient | None = None
        self._access_token: str = ""
        self._project_id: str = ""

    def xǁGcsConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None
        self._access_token: str = ""
        self._project_id: str = ""

    def xǁGcsConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._GCS_API_BASE
        self._http: HttpClient | None = ""
        self._access_token: str = ""
        self._project_id: str = ""

    def xǁGcsConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._GCS_API_BASE
        self._http: HttpClient | None = None
        self._access_token: str = None
        self._project_id: str = ""

    def xǁGcsConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._GCS_API_BASE
        self._http: HttpClient | None = None
        self._access_token: str = "XXXX"
        self._project_id: str = ""

    def xǁGcsConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._GCS_API_BASE
        self._http: HttpClient | None = None
        self._access_token: str = ""
        self._project_id: str = None

    def xǁGcsConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._GCS_API_BASE
        self._http: HttpClient | None = None
        self._access_token: str = ""
        self._project_id: str = "XXXX"

    # ── Conexion ────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁGcsConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXGcsConnector: credenciales no configuradasXX")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("gcsconnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GCSCONNECTOR: CREDENCIALES NO CONFIGURADAS")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return True

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = None
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(None, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, None) else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr("get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, ) else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "XXget_credentialsXX") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "GET_CREDENTIALS") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = None
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get(None, os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", None)
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get(os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", )
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("XXaccess_tokenXX", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("ACCESS_TOKEN", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get(None, ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", None))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get(""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("XXGCS_ACCESS_TOKENXX", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("gcs_access_token", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", "XXXX"))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = None

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get(None, os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", None)

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get(os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", )

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("XXproject_idXX", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("PROJECT_ID", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get(None, ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", None))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get(""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("XXGCP_PROJECT_IDXX", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("gcp_project_id", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", "XXXX"))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = None
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get(None, os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", None)
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get(os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", )
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("XXservice_account_jsonXX", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("SERVICE_ACCOUNT_JSON", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get(None, ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", None))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get(""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("XXGCS_SERVICE_ACCOUNT_JSONXX", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("gcs_service_account_json", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", "XXXX"))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token or sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = None

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(None)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error(None)
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("XXGcsConnector: access_token no proporcionadoXX")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("gcsconnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GCSCONNECTOR: ACCESS_TOKEN NO PROPORCIONADO")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return True

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = None
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=None,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=None,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=31,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth(None, token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=None)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth(token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", )

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("XXBearerXX", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("BEARER", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = None
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = None
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["XXprojectXX"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["PROJECT"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = None
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get(None, params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=None)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get(params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", )
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("XX/bXX", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/B", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = None
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = False
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation(None, f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", None)
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation(f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", )
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("XXconnectXX", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("CONNECT", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return False
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code != 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 404:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = None
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = False
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_107(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation(None, f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_108(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", None)
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_109(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation(f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_110(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", )
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_111(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("XXconnectXX", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_112(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("CONNECT", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_113(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return False
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_114(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(None)
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_115(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return True
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_116(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(None)
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁGcsConnectorǁconnect__mutmut_117(self) -> bool:
        """Establece conexion con Google Cloud Storage."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GcsConnector: credenciales no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("GCS_ACCESS_TOKEN", ""))
        self._project_id = creds.get("project_id", os.environ.get("GCP_PROJECT_ID", ""))

        # Si hay service_account_json, obtener access_token via JWT
        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if not self._access_token and sa_json:
            self._access_token = self._get_access_token_from_sa(sa_json)

        if not self._access_token:
            logger.error("GcsConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClient
        self._http = HttpClient(
            base_url=self._GCS_API_BASE,
            connector_name=self.name,
            timeout=30,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion listando buckets del proyecto
        try:
            params = {}
            if self._project_id:
                params["project"] = self._project_id
            resp = self._http.get("/b", params=params)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", f"GCS API OK, project={self._project_id}")
                return True
            # 403 puede significar que el token es valido pero sin permisos de list
            if resp.status_code == 403:
                self._connected = True
                self._log_operation("connect", f"GCS API OK (403 en list buckets, token valido), project={self._project_id}")
                return True
            logger.error(f"GcsConnector: GCS API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"GcsConnector: GCS API fallo: {exc}")
            return True

    @_mutmut_mutated(mutants_xǁGcsConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "XXupload_fileXX": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "UPLOAD_FILE": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "XXdownload_fileXX": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "DOWNLOAD_FILE": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "XXlist_objectsXX": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "LIST_OBJECTS": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "XXdelete_objectXX": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "DELETE_OBJECT": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "XXget_signed_urlXX": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "GET_SIGNED_URL": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "XXlist_bucketsXX": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "LIST_BUCKETS": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁGcsConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GCS."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_signed_url": self._get_signed_url,
            "list_buckets": self._list_buckets,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁGcsConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales de GCS esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGcsConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales de GCS esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGcsConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales de GCS esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGcsConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales de GCS esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁGcsConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = False
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = False
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = True
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = False
        self._http = ""
        self._log_operation("disconnect")
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = False
        self._http = None
        self._log_operation(None)
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = False
        self._http = None
        self._log_operation("XXdisconnectXX")
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = False
        self._http = None
        self._log_operation("DISCONNECT")
        return True

    def xǁGcsConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Google Cloud Storage."""
        self._connected = False
        self._http = None
        self._log_operation("disconnect")
        return False

    # ── Acciones ───────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_upload_file__mutmut)
    def _upload_file(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = None
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get(None, "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", None)
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", )
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("XXbucketXX", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("BUCKET", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "XXXX")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = None
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get(None, "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", None)
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", )
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("XXobject_nameXX", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("OBJECT_NAME", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "XXXX")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = None
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get(None)
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("XXcontentXX")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("CONTENT")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = None

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get(None)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("XXfile_pathXX")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("FILE_PATH")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket and not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": True, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "XXParametros requeridos: bucket, object_nameXX"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, OBJECT_NAME"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None or not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is not None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"XXsuccessXX": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"SUCCESS": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": True, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "XXerrorXX": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "ERROR": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "XXDebe proporcionar 'content' o 'file_path'XX"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "DEBE PROPORCIONAR 'CONTENT' O 'FILE_PATH'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None or file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is not None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(None, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, None) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open("rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, ) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "XXrbXX") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "RB") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = None
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"XXsuccessXX": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"SUCCESS": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": True, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "XXerrorXX": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "ERROR": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = None

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode(None)

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("XXutf-8XX")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("UTF-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = None
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get(None, "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", None)
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", )
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("XXcontent_typeXX", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("CONTENT_TYPE", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "XXapplication/octet-streamXX")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "APPLICATION/OCTET-STREAM")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation(None, f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation(f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("XXupload_fileXX", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("UPLOAD_FILE", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = None
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=None,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=None,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=None,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=31,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth(None, token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=None)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth(token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", )

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("XXBearerXX", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("BEARER", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = None

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                None,
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json=None,
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers=None,
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=None,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "XXnameXX": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "NAME": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "XXX-Upload-Content-TypeXX": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "x-upload-content-type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-UPLOAD-CONTENT-TYPE": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "XXX-Upload-Content-LengthXX": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "x-upload-content-length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-UPLOAD-CONTENT-LENGTH": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(None),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "XXContent-TypeXX": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "content-type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "CONTENT-TYPE": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "XXapplication/json; charset=UTF-8XX",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=utf-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "APPLICATION/JSON; CHARSET=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=31,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = None
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get(None, "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", None)
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", )
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("XXLocationXX", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("LOCATION", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "XXXX")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(None, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, None, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, None, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, None)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, )

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = None

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                None,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=None,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers=None,
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=None,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "XXContent-TypeXX": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "content-type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "CONTENT-TYPE": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "XXContent-LengthXX": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "content-length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "CONTENT-LENGTH": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(None),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=61,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 201 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 < put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code <= 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 301:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = None
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(None, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, None):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = None
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "XXsuccessXX": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "SUCCESS": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": False,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "XXbucketXX": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "BUCKET": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "XXobject_nameXX": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "OBJECT_NAME": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "XXgenerationXX": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "GENERATION": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(None),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get(None, "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", None)),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", )),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("XXgenerationXX", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("GENERATION", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "XXXX")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "XXsizeXX": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "SIZE": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"XXsuccessXX": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"SUCCESS": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": True, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "XXerrorXX": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "ERROR": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}
        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"XXsuccessXX": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"SUCCESS": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": True, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "XXerrorXX": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁGcsConnectorǁ_upload_file__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de GCS usando resumable upload.

        Args:
            params: Debe contener 'bucket', 'object_name' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, object={object_name}, size={len(content)}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Paso 1: Iniciar sesion de upload resumable
            upload_http = HttpClient(
                base_url=self._GCS_UPLOAD_BASE,
                connector_name=self.name,
                timeout=30,
            )
            upload_http.set_auth("Bearer", token=self._access_token)

            init_resp = upload_http.post(
                f"/b/{bucket}/o",
                json={
                    "name": object_name,
                },
                headers={
                    "X-Upload-Content-Type": content_type,
                    "X-Upload-Content-Length": str(len(content)),
                    "Content-Type": "application/json; charset=UTF-8",
                },
                timeout=30,
            )

            # Obtener la URI de upload del header Location
            upload_url = init_resp.headers.get("Location", "")
            if not upload_url:
                # Si no hay resumable, intentar upload simple (multipart)
                return self._upload_simple(bucket, object_name, content, content_type)

            # Paso 2: Subir el contenido a la URI de upload
            import requests as req_lib

            put_resp = req_lib.put(
                upload_url,
                data=content,
                headers={
                    "Content-Type": content_type,
                    "Content-Length": str(len(content)),
                },
                timeout=60,
            )

            if 200 <= put_resp.status_code < 300:
                result = {}
                with contextlib.suppress(json.JSONDecodeError, ValueError):
                    result = put_resp.json()
                return {
                    "success": True,
                    "bucket": bucket,
                    "object_name": object_name,
                    "generation": str(result.get("generation", "")),
                    "size": len(content),
                }
            return {"success": False, "error": f"Upload PUT fallo: HTTP {put_resp.status_code}"}

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}
        except Exception as exc:
            return {"success": False, "ERROR": f"Error inesperado: {exc}"}

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_upload_simple__mutmut)
    def _upload_simple(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_orig(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_1(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode(None)

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_2(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(None).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_3(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("XXasciiXX")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_4(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ASCII")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_5(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = None

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_6(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = None

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_7(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            None,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_8(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=None,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_9(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers=None,
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_10(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=None,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_11(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_12(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_13(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_14(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_15(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "XXAuthorizationXX": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_16(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_17(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "AUTHORIZATION": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_18(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "XXContent-TypeXX": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_19(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "content-type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_20(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "CONTENT-TYPE": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_21(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=61,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_22(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 201 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_23(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 < resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_24(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code <= 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_25(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 301:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_26(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = None
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_27(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(None, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_28(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, None):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_29(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_30(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_31(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = None
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_32(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "XXsuccessXX": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_33(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "SUCCESS": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_34(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": False,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_35(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "XXbucketXX": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_36(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "BUCKET": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_37(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "XXobject_nameXX": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_38(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "OBJECT_NAME": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_39(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "XXgenerationXX": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_40(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "GENERATION": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_41(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(None),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_42(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get(None, "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_43(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", None)),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_44(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_45(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", )),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_46(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("XXgenerationXX", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_47(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("GENERATION", "")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_48(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "XXXX")),
                "size": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_49(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "XXsizeXX": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_50(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "SIZE": len(content),
            }
        return {"success": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_51(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"XXsuccessXX": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_52(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"SUCCESS": False, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_53(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": True, "error": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_54(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "XXerrorXX": f"Upload simple fallo: HTTP {resp.status_code}"}

    def xǁGcsConnectorǁ_upload_simple__mutmut_55(
        self, bucket: str, object_name: str, content: bytes, content_type: str
    ) -> dict[str, Any]:
        """Upload simple (multipart/form-data) para archivos pequenos (< 5MB)."""
        import requests as req_lib

        # Codificar contenido en base64 para el JSON API
        base64.b64encode(content).decode("ascii")

        # Usar el endpoint de upload simple con media upload
        url = f"{self._GCS_UPLOAD_BASE}/b/{bucket}/o?uploadType=media&name={object_name}"

        resp = req_lib.post(
            url,
            data=content,
            headers={
                "Authorization": f"Bearer {self._access_token}",
                "Content-Type": content_type,
            },
            timeout=60,
        )

        if 200 <= resp.status_code < 300:
            result = {}
            with contextlib.suppress(json.JSONDecodeError, ValueError):
                result = resp.json()
            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "generation": str(result.get("generation", "")),
                "size": len(content),
            }
        return {"success": False, "ERROR": f"Upload simple fallo: HTTP {resp.status_code}"}

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_download_file__mutmut)
    def _download_file(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = None
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get(None, "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", None)
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", )
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("XXbucketXX", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("BUCKET", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "XXXX")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = None
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get(None, "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", None)
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", )
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("XXobject_nameXX", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("OBJECT_NAME", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "XXXX")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket and not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": True, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "XXParametros requeridos: bucket, object_nameXX"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, OBJECT_NAME"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation(None, f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation(f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("XXdownload_fileXX", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("DOWNLOAD_FILE", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = None
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(None)
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"SUCCESS": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": True, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "XXerrorXX": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "ERROR": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = None

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = None
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=None,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=None,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=None,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=61,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth(None, token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=None)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth(token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", )

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("XXBearerXX", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("BEARER", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = None

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                None,
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params=None,
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"XXaltXX": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"ALT": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "XXmediaXX"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "MEDIA"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = None
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b"XXXX"
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = None
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get(None, "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", None) if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", ) if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("XXcontentTypeXX", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contenttype", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("CONTENTTYPE", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "XXapplication/octet-streamXX") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "APPLICATION/OCTET-STREAM") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "XXapplication/octet-streamXX"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "APPLICATION/OCTET-STREAM"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = None

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get(None, len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", None) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get(len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", ) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("XXsizeXX", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("SIZE", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "XXsuccessXX": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "SUCCESS": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": False,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "XXbucketXX": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "BUCKET": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "XXobject_nameXX": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "OBJECT_NAME": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "XXcontentXX": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "CONTENT": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "XXsize_bytesXX": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "SIZE_BYTES": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(None) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "XXcontent_typeXX": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "CONTENT_TYPE": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "XXmetadataXX": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "METADATA": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁGcsConnectorǁ_download_file__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("download_file", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Obtener metadata del objeto
            metadata_resp = self._http.get(f"/b/{bucket}/o/{object_name}")
            if not metadata_resp.ok:
                return {"success": False, "error": f"HTTP {metadata_resp.status_code}: {metadata_resp.body}"}

            metadata = metadata_resp.json() if isinstance(metadata_resp.body, str) else metadata_resp.body

            # Descargar contenido usando alt=media
            download_http = HttpClient(
                base_url=self._GCS_API_BASE,
                connector_name=self.name,
                timeout=60,
            )
            download_http.set_auth("Bearer", token=self._access_token)

            content_resp = download_http.get(
                f"/b/{bucket}/o/{object_name}",
                params={"alt": "media"},
            )

            content = content_resp.raw if content_resp.raw else b""
            content_type = metadata.get("contentType", "application/octet-stream") if isinstance(metadata, dict) else "application/octet-stream"
            size = metadata.get("size", len(content)) if isinstance(metadata, dict) else len(content)

            return {
                "success": True,
                "bucket": bucket,
                "object_name": object_name,
                "content": content,
                "size_bytes": int(size) if isinstance(size, (int, str)) else len(content),
                "content_type": content_type,
                "metadata": metadata,
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_list_objects__mutmut)
    def _list_objects(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = None
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get(None, "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", None)
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", )
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("XXbucketXX", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("BUCKET", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "XXXX")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = None
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get(None, "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", None)
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", )
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("XXprefixXX", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("PREFIX", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "XXXX")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = None
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get(None, 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", None)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get(1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", )
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("XXlimitXX", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("LIMIT", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1001)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = None

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get(None, "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", None)

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", )

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("XXpage_tokenXX", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("PAGE_TOKEN", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "XXXX")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"XXsuccessXX": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"SUCCESS": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": True, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "XXerrorXX": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "ERROR": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "XXParametro requerido: bucketXX"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "PARAMETRO REQUERIDO: BUCKET"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation(None, f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation(f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("XXlist_objectsXX", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("LIST_OBJECTS", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = None
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"XXmaxResultsXX": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxresults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"MAXRESULTS": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = None
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["XXprefixXX"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["PREFIX"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = None

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["XXpageTokenXX"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pagetoken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["PAGETOKEN"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = None

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(None, params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=None)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = None
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"XXsuccessXX": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"SUCCESS": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": True, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "XXerrorXX": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "ERROR": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "XXRespuesta inesperada de GCS APIXX"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "respuesta inesperada de gcs api"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "RESPUESTA INESPERADA DE GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = None
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get(None, []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", None):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get([]):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", ):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("XXitemsXX", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("ITEMS", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append(None)

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "XXnameXX": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "NAME": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get(None, ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", None),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get(""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("XXnameXX", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("NAME", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", "XXXX"),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "XXsizeXX": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "SIZE": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get(None, "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", None),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", ),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("XXsizeXX", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("SIZE", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "XX0XX"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "XXcontent_typeXX": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "CONTENT_TYPE": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get(None, ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", None),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get(""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("XXcontentTypeXX", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contenttype", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("CONTENTTYPE", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", "XXXX"),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "XXupdatedXX": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "UPDATED": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get(None, ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", None),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get(""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("XXupdatedXX", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("UPDATED", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", "XXXX"),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "XXgenerationXX": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "GENERATION": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get(None, ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", None),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get(""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("XXgenerationXX", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("GENERATION", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", "XXXX"),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "XXmd5_hashXX": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "MD5_HASH": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get(None, ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", None),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get(""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("XXmd5HashXX", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("MD5HASH", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", "XXXX"),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "XXsuccessXX": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "SUCCESS": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": False,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "XXbucketXX": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "BUCKET": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "XXobjectsXX": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "OBJECTS": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "XXtotalXX": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "TOTAL": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "XXnext_page_tokenXX": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "NEXT_PAGE_TOKEN": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get(None, ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", None),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get(""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("XXnextPageTokenXX", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextpagetoken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("NEXTPAGETOKEN", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", "XXXX"),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "XXprefixesXX": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "PREFIXES": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get(None, []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", None),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get([]),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", ),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("XXprefixesXX", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("PREFIXES", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁGcsConnectorǁ_list_objects__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de GCS con paginacion.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'page_token'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        page_token = params.get("page_token", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"maxResults": limit}
            if prefix:
                query_params["prefix"] = prefix
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(f"/b/{bucket}/o", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            objects = []
            for item in body.get("items", []):
                objects.append({
                    "name": item.get("name", ""),
                    "size": item.get("size", "0"),
                    "content_type": item.get("contentType", ""),
                    "updated": item.get("updated", ""),
                    "generation": item.get("generation", ""),
                    "md5_hash": item.get("md5Hash", ""),
                })

            return {
                "success": True,
                "bucket": bucket,
                "objects": objects,
                "total": len(objects),
                "next_page_token": body.get("nextPageToken", ""),
                "prefixes": body.get("prefixes", []),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_delete_object__mutmut)
    def _delete_object(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = None
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get(None, "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", None)
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", )
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("XXbucketXX", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("BUCKET", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "XXXX")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = None
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get(None, "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", None)
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", )
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("XXobject_nameXX", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("OBJECT_NAME", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "XXXX")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket and not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": True, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "XXParametros requeridos: bucket, object_nameXX"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, OBJECT_NAME"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation(None, f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation(f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("XXdelete_objectXX", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("DELETE_OBJECT", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = None
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(None)
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok and resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code != 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 205:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"XXsuccessXX": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"SUCCESS": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": False, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "XXdeletedXX": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "DELETED": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": False, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "XXbucketXX": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "BUCKET": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "XXobject_nameXX": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "OBJECT_NAME": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁGcsConnectorǁ_delete_object__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de GCS.

        Args:
            params: Debe contener 'bucket' y 'object_name'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("delete_object", f"bucket={bucket}, object={object_name}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.delete(f"/b/{bucket}/o/{object_name}")
            if resp.ok or resp.status_code == 204:
                return {"success": True, "deleted": True, "bucket": bucket, "object_name": object_name}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_get_signed_url__mutmut)
    def _get_signed_url(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = None
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get(None, "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", None)
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", )
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("XXbucketXX", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("BUCKET", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "XXXX")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = None
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get(None, "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", None)
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", )
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("XXobject_nameXX", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("OBJECT_NAME", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "XXXX")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = None

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get(None, 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", None)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get(3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", )

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("XXexpires_inXX", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("EXPIRES_IN", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3601)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket and not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": True, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "XXParametros requeridos: bucket, object_nameXX"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, OBJECT_NAME"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation(None, f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", None)

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation(f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", )

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("XXget_signed_urlXX", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("GET_SIGNED_URL", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = None
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider or hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(None, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, None):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr("get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, ):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "XXget_credentialsXX"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "GET_CREDENTIALS"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = None

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = None
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get(None, os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", None)
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get(os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", )
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("XXservice_account_jsonXX", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("SERVICE_ACCOUNT_JSON", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get(None, ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", None))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get(""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("XXGCS_SERVICE_ACCOUNT_JSONXX", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("gcs_service_account_json", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", "XXXX"))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = None
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(None, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, None, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, None, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, None)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, )
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"XXsuccessXX": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"SUCCESS": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": False, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "XXurlXX": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "URL": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "XXexpires_inXX": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "EXPIRES_IN": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(None)

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = None
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"XXsuccessXX": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"SUCCESS": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": False, "url": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "XXurlXX": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "URL": url, "expires_in": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "XXexpires_inXX": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "EXPIRES_IN": expires_in, "note": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "XXnoteXX": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "NOTE": "URL sin firmar (requiere service_account_json para URL firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "XXURL sin firmar (requiere service_account_json para URL firmada)XX"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "url sin firmar (requiere service_account_json para url firmada)"}

    def xǁGcsConnectorǁ_get_signed_url__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de GCS.

        Args:
            params: Debe contener 'bucket', 'object_name' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        object_name = params.get("object_name", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not object_name:
            return {"success": False, "error": "Parametros requeridos: bucket, object_name"}

        self._log_operation("get_signed_url", f"bucket={bucket}, object={object_name}")

        # Para generar URLs firmadas reales se necesita una service account
        # con private key. Aqui generamos la URL directa como fallback.
        # Si se tiene service_account_json, se puede firmar correctamente.
        creds = {}
        if self._auth_provider and hasattr(self._auth_provider, "get_credentials"):
            creds = self._auth_provider.get_credentials()

        sa_json = creds.get("service_account_json", os.environ.get("GCS_SERVICE_ACCOUNT_JSON", ""))
        if sa_json:
            try:
                signed_url = self._create_signed_url(bucket, object_name, expires_in, sa_json)
                return {"success": True, "url": signed_url, "expires_in": expires_in}
            except Exception as exc:
                logger.warning(f"GcsConnector: error generando signed URL: {exc}")

        # Fallback: URL directa (solo funciona para objetos publicos)
        url = f"https://storage.googleapis.com/{bucket}/{object_name}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL SIN FIRMAR (REQUIERE SERVICE_ACCOUNT_JSON PARA URL FIRMADA)"}

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_list_buckets__mutmut)
    def _list_buckets(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = None
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get(None, self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", None)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get(self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", )
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("XXproject_idXX", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("PROJECT_ID", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = None

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get(None, "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", None)

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", )

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("XXpage_tokenXX", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("PAGE_TOKEN", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "XXXX")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"SUCCESS": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": True, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "ERROR": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "XXParametro requerido: project_id (o configurar en connect)XX"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PROJECT_ID (O CONFIGURAR EN CONNECT)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation(None, f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation(f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("XXlist_bucketsXX", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("LIST_BUCKETS", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = None
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"XXprojectXX": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"PROJECT": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = None

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["XXpageTokenXX"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pagetoken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["PAGETOKEN"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = None

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(None, params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=None)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get(params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("XX/bXX", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/B", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = None
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"XXsuccessXX": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"SUCCESS": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": True, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "XXerrorXX": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "ERROR": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "XXRespuesta inesperada de GCS APIXX"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "respuesta inesperada de gcs api"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "RESPUESTA INESPERADA DE GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = None
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get(None, []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", None):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get([]):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", ):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("XXitemsXX", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("ITEMS", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append(None)

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "XXnameXX": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "NAME": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get(None, ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", None),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get(""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("XXnameXX", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("NAME", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", "XXXX"),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "XXlocationXX": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "LOCATION": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get(None, ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", None),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get(""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("XXlocationXX", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("LOCATION", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", "XXXX"),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "XXstorage_classXX": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "STORAGE_CLASS": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get(None, ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", None),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get(""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("XXstorageClassXX", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageclass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("STORAGECLASS", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", "XXXX"),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "XXcreatedXX": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "CREATED": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get(None, ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", None),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get(""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("XXtimeCreatedXX", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timecreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("TIMECREATED", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", "XXXX"),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "XXsuccessXX": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "SUCCESS": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": False,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "XXbucketsXX": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "BUCKETS": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "XXproject_idXX": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "PROJECT_ID": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "XXnext_page_tokenXX": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "NEXT_PAGE_TOKEN": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get(None, ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", None),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get(""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("XXnextPageTokenXX", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextpagetoken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("NEXTPAGETOKEN", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", "XXXX"),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁGcsConnectorǁ_list_buckets__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los buckets de GCS del proyecto con paginacion.

        Args:
            params: Opcionalmente 'project_id' y 'page_token'
        """
        project_id = params.get("project_id", self._project_id)
        page_token = params.get("page_token", "")

        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id (o configurar en connect)"}

        self._log_operation("list_buckets", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            query_params: dict[str, Any] = {"project": project_id}
            if page_token:
                query_params["pageToken"] = page_token

            resp = self._http.get("/b", params=query_params)

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de GCS API"}

            buckets = []
            for item in body.get("items", []):
                buckets.append({
                    "name": item.get("name", ""),
                    "location": item.get("location", ""),
                    "storage_class": item.get("storageClass", ""),
                    "created": item.get("timeCreated", ""),
                })

            return {
                "success": True,
                "buckets": buckets,
                "project_id": project_id,
                "next_page_token": body.get("nextPageToken", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    # ── Helpers ─────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut)
    def _get_access_token_from_sa(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_orig(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_1(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = None
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_2(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(None)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_3(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = None
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_4(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get(None, "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_5(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", None)
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_6(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_7(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", )
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_8(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("XXclient_emailXX", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_9(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("CLIENT_EMAIL", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_10(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "XXXX")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_11(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = None
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_12(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get(None, "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_13(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", None)
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_14(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_15(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", )
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_16(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("XXprivate_keyXX", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_17(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("PRIVATE_KEY", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_18(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "XXXX")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_19(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = None

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_20(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get(None, "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_21(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", None)

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_22(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_23(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", )

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_24(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("XXtoken_uriXX", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_25(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("TOKEN_URI", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_26(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "XXhttps://oauth2.googleapis.com/tokenXX")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_27(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "HTTPS://OAUTH2.GOOGLEAPIS.COM/TOKEN")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_28(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email and not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_29(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_30(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_31(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError(None)

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_32(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("XXService account JSON debe contener client_email y private_keyXX")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_33(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("service account json debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_34(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("SERVICE ACCOUNT JSON DEBE CONTENER CLIENT_EMAIL Y PRIVATE_KEY")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_35(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = None
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_36(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(None)
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_37(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = None

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_38(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "XXissXX": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_39(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "ISS": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_40(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "XXscopeXX": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_41(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "SCOPE": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_42(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "XXhttps://www.googleapis.com/auth/devstorage.read_writeXX",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_43(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "HTTPS://WWW.GOOGLEAPIS.COM/AUTH/DEVSTORAGE.READ_WRITE",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_44(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "XXaudXX": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_45(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "AUD": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_46(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "XXiatXX": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_47(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "IAT": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_48(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "XXexpXX": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_49(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "EXP": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_50(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now - 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_51(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3601,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_52(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = None
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_53(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip(None)
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_54(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().lstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_55(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(None).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_56(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps(None).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_57(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"XXalgXX": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_58(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"ALG": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_59(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "XXRS256XX", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_60(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "rs256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_61(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "XXtypXX": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_62(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "TYP": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_63(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "XXJWTXX"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_64(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "jwt"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_65(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("XX=XX")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_66(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = None
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_67(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip(None)
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_68(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().lstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_69(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(None).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_70(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(None).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_71(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("XX=XX")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_72(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = None

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_73(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = None
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_74(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                None, password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_75(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_76(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_77(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = None
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_78(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                None,
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_79(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                None,
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_80(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                None,
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_81(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_82(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_83(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_84(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = None
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_85(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip(None)
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_86(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().lstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_87(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(None).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_88(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("XX=XX")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_89(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = None
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_90(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = None
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_91(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(None, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_92(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, None, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_93(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm=None)
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_94(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_95(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_96(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, )
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_97(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="XXRS256XX")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_98(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="rs256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_99(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError(None) from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_100(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("XXSe necesita 'cryptography' o 'pyjwt' para firmar JWT con service accountXX") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_101(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("se necesita 'cryptography' o 'pyjwt' para firmar jwt con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_102(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("SE NECESITA 'CRYPTOGRAPHY' O 'PYJWT' PARA FIRMAR JWT CON SERVICE ACCOUNT") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_103(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = None

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_104(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            None,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_105(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data=None,
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_106(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=None,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_107(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_108(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_109(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_110(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "XXgrant_typeXX": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_111(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "GRANT_TYPE": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_112(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "XXurn:ietf:params:oauth:grant-type:jwt-bearerXX",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_113(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "URN:IETF:PARAMS:OAUTH:GRANT-TYPE:JWT-BEARER",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_114(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "XXassertionXX": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_115(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "ASSERTION": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_116(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=31,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_117(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code == 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_118(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 201:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_119(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(None)

        return resp.json().get("access_token", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_120(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get(None, "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_121(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", None)

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_122(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_123(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", )

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_124(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("XXaccess_tokenXX", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_125(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("ACCESS_TOKEN", "")

    # ── Helpers ─────────────────────────────────────────────────

    def xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_126(self, sa_json_str: str) -> str:
        """Obtiene un access token de Google OAuth2 usando una Service Account JSON.

        Args:
            sa_json_str: JSON string de la service account

        Returns:
            Access token de Google OAuth2
        """
        import requests as req_lib

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")
        token_uri = sa_info.get("token_uri", "https://oauth2.googleapis.com/token")

        if not client_email or not private_key:
            raise ValueError("Service account JSON debe contener client_email y private_key")

        # Crear JWT
        now = int(time.time())
        payload = {
            "iss": client_email,
            "scope": "https://www.googleapis.com/auth/devstorage.read_write",
            "aud": token_uri,
            "iat": now,
            "exp": now + 3600,
        }

        # Codificar JWT manualmente (sin dependencia de pyjwt)
        header_b64 = base64.urlsafe_b64encode(json.dumps({"alg": "RS256", "typ": "JWT"}).encode()).decode().rstrip("=")
        payload_b64 = base64.urlsafe_b64encode(json.dumps(payload).encode()).decode().rstrip("=")
        sign_input = f"{header_b64}.{payload_b64}"

        # Firmar con RSA
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                sign_input.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_b64 = base64.urlsafe_b64encode(signature).decode().rstrip("=")
            jwt_token = f"{sign_input}.{sig_b64}"
        except ImportError:
            # Fallback sin cryptography — usar pyjwt si esta disponible
            try:
                import jwt
                jwt_token = jwt.encode(payload, private_key, algorithm="RS256")
            except ImportError:
                raise ValueError("Se necesita 'cryptography' o 'pyjwt' para firmar JWT con service account") from None

        # Intercambiar JWT por access token
        resp = req_lib.post(
            token_uri,
            data={
                "grant_type": "urn:ietf:params:oauth:grant-type:jwt-bearer",
                "assertion": jwt_token,
            },
            timeout=30,
        )

        if resp.status_code != 200:
            raise ValueError(f"Error obteniendo access token: {resp.status_code} {resp.text}")

        return resp.json().get("access_token", "XXXX")

    @_mutmut_mutated(mutants_xǁGcsConnectorǁ_create_signed_url__mutmut)
    def _create_signed_url(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_orig(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_1(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = None
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_2(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(None)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_3(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = None
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_4(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get(None, "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_5(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", None)
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_6(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_7(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", )
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_8(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("XXclient_emailXX", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_9(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("CLIENT_EMAIL", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_10(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "XXXX")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_11(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = None

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_12(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get(None, "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_13(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", None)

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_14(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_15(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", )

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_16(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("XXprivate_keyXX", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_17(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("PRIVATE_KEY", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_18(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "XXXX")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_19(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = None
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_20(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(None)
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_21(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now - expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_22(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = None
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_23(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime(None, time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_24(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", None)
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_25(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime(time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_26(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", )
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_27(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("XX%Y%m%dXX", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_28(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_29(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%M%D", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_30(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(None))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_31(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = None
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_32(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime(None, time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_33(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", None)
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_34(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime(time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_35(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", )
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_36(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("XX%Y%m%dT%H%M%SZXX", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_37(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%y%m%dt%h%m%sz", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_38(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%M%DT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_39(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(None))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_40(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = None

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_41(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = None

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_42(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(None, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_43(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe=None)

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_44(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_45(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, )

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_46(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="XXXX")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_47(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = None

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_48(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join(None)

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_49(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "XX\nXX".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_50(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "XXGETXX",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_51(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "get",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_52(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(None)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_53(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' - credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_54(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email - '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_55(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + 'XX/XX' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_56(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "XXhost:storage.googleapis.comXX",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_57(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "HOST:STORAGE.GOOGLEAPIS.COM",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_58(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "XXXX",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_59(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "XXhostXX",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_60(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "HOST",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_61(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "XXUNSIGNED-PAYLOADXX",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_62(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "unsigned-payload",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_63(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = None

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_64(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join(None)

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_65(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "XX\nXX".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_66(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "XXGOOG4-RSA-SHA256XX",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_67(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "goog4-rsa-sha256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_68(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(None).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_69(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = None
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_70(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                None, password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_71(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_72(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_73(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = None
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_74(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                None,
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_75(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                None,
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_76(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                None,
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_77(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_78(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_79(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_80(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = None
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_81(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip(None)
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_82(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().lstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_83(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(None).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_84(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("XX=XX")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_85(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError(None) from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_86(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("XXSe necesita 'cryptography' para generar URLs firmadasXX") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_87(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("se necesita 'cryptography' para generar urls firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_88(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("SE NECESITA 'CRYPTOGRAPHY' PARA GENERAR URLS FIRMADAS") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_89(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = None

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_90(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(None)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_91(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + '/' - credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_92(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email - '/' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

    def xǁGcsConnectorǁ_create_signed_url__mutmut_93(
        self, bucket: str, object_name: str, expires_in: int, sa_json_str: str
    ) -> str:
        """Crea una URL firmada V4 para un objeto de GCS.

        Args:
            bucket: Nombre del bucket
            object_name: Nombre del objeto
            expires_in: Tiempo de expiracion en segundos
            sa_json_str: JSON string de la service account

        Returns:
            URL firmada
        """
        import hashlib
        from urllib.parse import quote

        sa_info = json.loads(sa_json_str)
        client_email = sa_info.get("client_email", "")
        private_key = sa_info.get("private_key", "")

        now = int(time.time())
        now + expires_in

        # Construct the string to sign (V4 signing)
        date_stamp = time.strftime("%Y%m%d", time.gmtime(now))
        time_stamp = time.strftime("%Y%m%dT%H%M%SZ", time.gmtime(now))
        credential_scope = f"{date_stamp}/auto/storage/goog4_request"

        encoded_object = quote(object_name, safe="")

        canonical_request = "\n".join([
            "GET",
            f"/{bucket}/{encoded_object}",
            f"X-Goog-Algorithm=GOOG4-RSA-SHA256&X-Goog-Credential={quote(client_email + '/' + credential_scope)}&X-Goog-Date={time_stamp}&X-Goog-Expires={expires_in}&X-Goog-SignedHeaders=host",
            "host:storage.googleapis.com",
            "",
            "host",
            "UNSIGNED-PAYLOAD",
        ])

        string_to_sign = "\n".join([
            "GOOG4-RSA-SHA256",
            time_stamp,
            credential_scope,
            hashlib.sha256(canonical_request.encode()).hexdigest(),
        ])

        # Firmar
        try:
            from cryptography.hazmat.primitives import hashes, serialization
            from cryptography.hazmat.primitives.asymmetric import padding

            private_key_obj = serialization.load_pem_private_key(
                private_key.encode(), password=None
            )
            signature = private_key_obj.sign(
                string_to_sign.encode(),
                padding.PKCS1v15(),
                hashes.SHA256(),
            )
            sig_encoded = base64.urlsafe_b64encode(signature).decode().rstrip("=")
        except ImportError:
            raise ValueError("Se necesita 'cryptography' para generar URLs firmadas") from None

        signed_url = (
            f"https://storage.googleapis.com/{bucket}/{encoded_object}"
            f"?X-Goog-Algorithm=GOOG4-RSA-SHA256"
            f"&X-Goog-Credential={quote(client_email + 'XX/XX' + credential_scope)}"
            f"&X-Goog-Date={time_stamp}"
            f"&X-Goog-Expires={expires_in}"
            f"&X-Goog-SignedHeaders=host"
            f"&X-Goog-Signature={sig_encoded}"
        )

        return signed_url

mutants_xǁGcsConnectorǁ__init____mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ__init____mutmut['xǁGcsConnectorǁ__init____mutmut_1'] = GcsConnector.xǁGcsConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ__init____mutmut['xǁGcsConnectorǁ__init____mutmut_2'] = GcsConnector.xǁGcsConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ__init____mutmut['xǁGcsConnectorǁ__init____mutmut_3'] = GcsConnector.xǁGcsConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ__init____mutmut['xǁGcsConnectorǁ__init____mutmut_4'] = GcsConnector.xǁGcsConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ__init____mutmut['xǁGcsConnectorǁ__init____mutmut_5'] = GcsConnector.xǁGcsConnectorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ__init____mutmut['xǁGcsConnectorǁ__init____mutmut_6'] = GcsConnector.xǁGcsConnectorǁ__init____mutmut_6 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁconnect__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_1'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_2'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_3'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_4'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_5'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_6'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_7'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_8'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_9'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_10'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_11'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_12'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_13'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_14'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_15'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_16'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_17'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_18'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_19'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_20'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_21'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_22'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_23'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_24'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_25'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_26'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_27'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_28'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_29'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_30'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_31'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_32'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_33'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_34'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_35'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_36'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_37'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_38'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_39'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_40'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_41'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_42'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_43'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_44'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_45'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_46'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_47'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_48'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_49'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_50'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_51'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_52'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_53'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_54'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_55'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_56'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_57'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_58'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_59'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_60'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_61'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_62'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_63'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_64'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_65'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_66'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_67'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_68'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_69'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_70'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_71'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_72'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_73'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_74'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_75'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_76'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_77'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_78'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_79'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_80'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_81'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_82'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_83'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_84'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_85'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_86'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_87'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_88'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_89'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_90'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_91'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_92'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_93'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_94'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_94 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_95'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_95 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_96'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_96 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_97'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_97 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_98'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_98 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_99'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_99 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_100'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_100 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_101'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_101 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_102'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_102 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_103'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_103 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_104'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_104 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_105'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_105 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_106'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_106 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_107'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_107 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_108'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_108 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_109'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_109 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_110'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_110 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_111'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_111 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_112'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_112 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_113'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_113 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_114'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_114 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_115'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_115 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_116'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_116 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁconnect__mutmut['xǁGcsConnectorǁconnect__mutmut_117'] = GcsConnector.xǁGcsConnectorǁconnect__mutmut_117 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁexecute__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_1'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_2'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_3'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_4'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_5'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_6'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_7'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_8'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_9'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_10'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_11'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_12'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_13'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_14'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_15'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_16'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_17'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_18'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_19'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_20'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_21'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁexecute__mutmut['xǁGcsConnectorǁexecute__mutmut_22'] = GcsConnector.xǁGcsConnectorǁexecute__mutmut_22 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁvalidate__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁvalidate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁvalidate__mutmut['xǁGcsConnectorǁvalidate__mutmut_1'] = GcsConnector.xǁGcsConnectorǁvalidate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁvalidate__mutmut['xǁGcsConnectorǁvalidate__mutmut_2'] = GcsConnector.xǁGcsConnectorǁvalidate__mutmut_2 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁdisconnect__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁdisconnect__mutmut['xǁGcsConnectorǁdisconnect__mutmut_1'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁdisconnect__mutmut['xǁGcsConnectorǁdisconnect__mutmut_2'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁdisconnect__mutmut['xǁGcsConnectorǁdisconnect__mutmut_3'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁdisconnect__mutmut['xǁGcsConnectorǁdisconnect__mutmut_4'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁdisconnect__mutmut['xǁGcsConnectorǁdisconnect__mutmut_5'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁdisconnect__mutmut['xǁGcsConnectorǁdisconnect__mutmut_6'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁdisconnect__mutmut['xǁGcsConnectorǁdisconnect__mutmut_7'] = GcsConnector.xǁGcsConnectorǁdisconnect__mutmut_7 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_upload_file__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_69'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_70'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_71'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_72'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_73'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_74'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_75'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_76'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_77'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_78'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_79'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_80'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_81'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_82'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_83'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_84'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_85'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_86'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_87'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_88'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_89'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_90'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_91'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_92'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_93'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_94'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_94 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_95'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_95 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_96'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_96 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_97'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_97 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_98'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_98 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_99'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_99 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_100'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_100 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_101'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_101 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_102'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_102 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_103'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_103 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_104'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_104 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_105'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_105 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_106'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_106 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_107'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_107 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_108'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_108 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_109'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_109 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_110'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_110 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_111'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_111 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_112'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_112 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_113'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_113 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_114'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_114 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_115'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_115 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_116'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_116 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_117'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_117 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_118'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_118 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_119'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_119 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_120'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_120 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_121'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_121 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_122'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_122 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_123'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_123 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_124'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_124 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_125'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_125 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_126'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_126 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_127'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_127 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_128'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_128 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_129'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_129 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_130'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_130 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_131'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_131 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_132'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_132 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_133'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_133 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_134'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_134 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_135'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_135 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_136'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_136 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_137'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_137 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_138'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_138 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_139'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_139 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_140'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_140 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_141'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_141 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_142'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_142 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_143'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_143 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_144'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_144 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_145'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_145 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_146'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_146 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_147'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_147 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_148'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_148 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_149'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_149 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_150'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_150 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_151'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_151 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_152'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_152 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_153'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_153 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_154'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_154 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_155'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_155 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_156'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_156 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_157'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_157 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_158'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_158 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_159'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_159 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_160'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_160 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_161'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_161 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_162'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_162 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_163'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_163 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_164'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_164 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_165'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_165 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_166'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_166 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_167'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_167 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_168'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_168 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_169'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_169 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_170'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_170 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_171'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_171 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_172'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_172 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_173'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_173 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_174'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_174 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_175'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_175 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_176'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_176 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_177'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_177 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_178'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_178 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_179'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_179 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_180'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_180 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_181'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_181 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_182'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_182 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_183'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_183 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_184'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_184 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_185'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_185 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_186'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_186 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_187'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_187 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_188'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_188 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_189'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_189 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_190'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_190 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_191'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_191 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_192'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_192 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_193'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_193 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_194'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_194 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_195'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_195 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_196'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_196 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_197'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_197 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_198'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_198 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_199'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_199 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_200'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_200 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_201'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_201 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_202'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_202 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_203'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_203 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_204'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_204 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_205'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_205 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_206'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_206 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_207'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_207 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_file__mutmut['xǁGcsConnectorǁ_upload_file__mutmut_208'] = GcsConnector.xǁGcsConnectorǁ_upload_file__mutmut_208 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_upload_simple__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_upload_simple__mutmut['xǁGcsConnectorǁ_upload_simple__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_upload_simple__mutmut_55 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_download_file__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_69'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_70'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_71'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_72'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_73'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_74'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_75'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_76'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_77'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_78'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_79'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_80'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_81'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_82'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_83'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_84'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_85'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_86'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_87'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_88'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_89'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_90'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_91'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_92'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_93'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_94'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_94 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_95'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_95 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_96'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_96 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_97'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_97 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_98'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_98 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_99'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_99 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_100'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_100 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_101'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_101 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_102'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_102 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_103'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_103 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_104'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_104 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_105'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_105 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_106'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_106 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_107'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_107 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_108'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_108 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_109'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_109 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_110'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_110 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_111'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_111 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_112'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_112 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_113'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_113 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_114'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_114 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_115'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_115 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_116'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_116 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_117'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_117 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_download_file__mutmut['xǁGcsConnectorǁ_download_file__mutmut_118'] = GcsConnector.xǁGcsConnectorǁ_download_file__mutmut_118 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_list_objects__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_69'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_70'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_71'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_72'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_73'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_74'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_75'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_76'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_77'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_78'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_79'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_80'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_81'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_82'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_83'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_84'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_85'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_86'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_87'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_88'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_89'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_90'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_91'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_92'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_93'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_94'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_94 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_95'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_95 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_96'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_96 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_97'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_97 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_98'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_98 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_99'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_99 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_100'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_100 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_101'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_101 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_102'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_102 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_103'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_103 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_104'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_104 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_105'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_105 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_106'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_106 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_107'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_107 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_108'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_108 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_109'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_109 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_110'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_110 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_111'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_111 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_112'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_112 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_113'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_113 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_114'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_114 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_115'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_115 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_116'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_116 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_117'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_117 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_118'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_118 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_119'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_119 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_120'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_120 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_121'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_121 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_122'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_122 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_123'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_123 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_124'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_124 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_125'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_125 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_126'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_126 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_127'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_127 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_128'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_128 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_129'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_129 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_130'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_130 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_131'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_131 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_132'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_132 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_133'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_133 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_134'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_134 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_135'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_135 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_136'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_136 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_137'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_137 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_138'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_138 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_139'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_139 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_140'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_140 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_141'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_141 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_142'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_142 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_143'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_143 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_144'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_144 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_145'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_145 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_146'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_146 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_147'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_147 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_148'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_148 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_149'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_149 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_150'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_150 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_151'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_151 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_152'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_152 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_153'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_153 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_154'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_154 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_155'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_155 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_156'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_156 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_157'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_157 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_158'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_158 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_159'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_159 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_160'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_160 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_161'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_161 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_162'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_162 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_163'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_163 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_164'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_164 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_165'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_165 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_166'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_166 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_167'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_167 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_168'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_168 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_169'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_169 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_170'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_170 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_171'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_171 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_172'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_172 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_173'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_173 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_174'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_174 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_175'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_175 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_176'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_176 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_177'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_177 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_178'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_178 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_179'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_179 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_180'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_180 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_181'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_181 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_182'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_182 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_183'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_183 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_184'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_184 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_objects__mutmut['xǁGcsConnectorǁ_list_objects__mutmut_185'] = GcsConnector.xǁGcsConnectorǁ_list_objects__mutmut_185 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_delete_object__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_delete_object__mutmut['xǁGcsConnectorǁ_delete_object__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_delete_object__mutmut_68 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_69'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_70'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_71'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_72'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_73'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_74'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_75'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_76'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_77'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_78'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_79'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_80'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_81'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_82'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_83'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_84'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_85'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_86'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_87'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_88'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_89'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_90'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_91'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_92'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_93'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_signed_url__mutmut['xǁGcsConnectorǁ_get_signed_url__mutmut_94'] = GcsConnector.xǁGcsConnectorǁ_get_signed_url__mutmut_94 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_list_buckets__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_69'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_70'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_71'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_72'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_73'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_74'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_75'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_76'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_77'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_78'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_79'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_80'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_81'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_82'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_83'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_84'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_85'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_86'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_87'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_88'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_89'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_90'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_91'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_92'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_93'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_94'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_94 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_95'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_95 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_96'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_96 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_97'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_97 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_98'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_98 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_99'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_99 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_100'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_100 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_101'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_101 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_102'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_102 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_103'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_103 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_104'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_104 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_105'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_105 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_106'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_106 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_107'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_107 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_108'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_108 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_109'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_109 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_110'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_110 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_111'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_111 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_112'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_112 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_113'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_113 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_114'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_114 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_115'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_115 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_116'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_116 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_117'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_117 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_118'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_118 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_119'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_119 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_120'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_120 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_121'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_121 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_122'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_122 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_123'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_123 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_124'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_124 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_125'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_125 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_126'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_126 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_127'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_127 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_128'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_128 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_129'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_129 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_130'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_130 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_131'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_131 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_132'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_132 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_133'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_133 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_134'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_134 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_135'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_135 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_136'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_136 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_137'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_137 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_list_buckets__mutmut['xǁGcsConnectorǁ_list_buckets__mutmut_138'] = GcsConnector.xǁGcsConnectorǁ_list_buckets__mutmut_138 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_69'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_70'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_71'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_72'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_73'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_74'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_75'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_76'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_77'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_78'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_79'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_80'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_81'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_82'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_83'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_84'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_85'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_86'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_87'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_88'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_89'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_90'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_91'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_92'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_93'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_94'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_94 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_95'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_95 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_96'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_96 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_97'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_97 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_98'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_98 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_99'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_99 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_100'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_100 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_101'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_101 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_102'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_102 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_103'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_103 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_104'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_104 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_105'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_105 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_106'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_106 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_107'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_107 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_108'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_108 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_109'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_109 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_110'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_110 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_111'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_111 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_112'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_112 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_113'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_113 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_114'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_114 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_115'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_115 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_116'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_116 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_117'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_117 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_118'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_118 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_119'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_119 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_120'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_120 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_121'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_121 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_122'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_122 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_123'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_123 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_124'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_124 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_125'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_125 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_get_access_token_from_sa__mutmut['xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_126'] = GcsConnector.xǁGcsConnectorǁ_get_access_token_from_sa__mutmut_126 # type: ignore # mutmut generated

mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['_mutmut_orig'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_1'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_2'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_3'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_4'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_5'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_6'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_6 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_7'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_7 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_8'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_8 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_9'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_9 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_10'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_10 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_11'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_11 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_12'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_12 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_13'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_13 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_14'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_14 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_15'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_15 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_16'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_16 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_17'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_17 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_18'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_18 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_19'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_19 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_20'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_21'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_22'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_23'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_24'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_25'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_26'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_27'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_28'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_29'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_30'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_31'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_32'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_33'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_34'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_35'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_36'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_37'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_38'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_39'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_40'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_41'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_42'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_43'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_44'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_45'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_46'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_47'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_48'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_49'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_50'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_51'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_52'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_53'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_54'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_55'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_56'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_57'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_58'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_59'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_60'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_61'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_62'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_63'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_64'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_65'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_66'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_67'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_68'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_69'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_70'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_71'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_72'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_73'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_74'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_75'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_76'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_77'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_78'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_79'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_80'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_81'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_82'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_83'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_84'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_85'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_86'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_87'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_88'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_89'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_90'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_91'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_92'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGcsConnectorǁ_create_signed_url__mutmut['xǁGcsConnectorǁ_create_signed_url__mutmut_93'] = GcsConnector.xǁGcsConnectorǁ_create_signed_url__mutmut_93 # type: ignore # mutmut generated


GCS_SCHEMA = ConnectorSchema(
    name="gcs",
    version="1.0.0",
    description="Sube, descarga y gestiona archivos en buckets de Google Cloud Storage",
    category="cloud_storage",
    icon="cloud",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="upload_file", description="Sube un archivo a GCS", category="write"),
        ActionDefinition(name="download_file", description="Descarga un archivo de GCS", category="read"),
        ActionDefinition(name="list_objects", description="Lista objetos en un bucket", category="read"),
        ActionDefinition(name="delete_object", description="Elimina un objeto de GCS", category="delete"),
        ActionDefinition(name="get_signed_url", description="Genera URL firmada", category="read"),
        ActionDefinition(name="list_buckets", description="Lista buckets del proyecto", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="oauth2", required_fields=["client_email", "private_key"], description="Service Account de GCP")
    ],
)
