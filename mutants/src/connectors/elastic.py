"""
Conector Elasticsearch — Busqueda e Indexacion
==================================================

Permite indexar documentos, ejecutar busquedas, gestionar
indices y analisis en clusters de Elasticsearch usando
la REST API nativa via HttpClient.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁElasticConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁ_index_document__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁ_search__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁ_get_document__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁ_delete_document__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁ_create_index__mutmut: MutantDict = {}  # type: ignore
mutants_xǁElasticConnectorǁ_list_indices__mutmut: MutantDict = {}  # type: ignore


class ElasticConnector(BaseConnector):
    """Conector para Elasticsearch: busqueda, indexacion y gestion de indices."""

    name = "elastic"
    version = "1.0.0"
    description = "Indexa documentos y ejecuta busquedas en Elasticsearch"
    category = "databases"
    icon = "search"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁElasticConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "http://localhost:9200"
        self._http: HttpClient | None = None

    def xǁElasticConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "http://localhost:9200"
        self._http: HttpClient | None = None

    def xǁElasticConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁElasticConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttp://localhost:9200XX"
        self._http: HttpClient | None = None

    def xǁElasticConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTP://LOCALHOST:9200"
        self._http: HttpClient | None = None

    def xǁElasticConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "http://localhost:9200"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁElasticConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXElasticConnector: credenciales no configuradasXX")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("elasticconnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ELASTICCONNECTOR: CREDENCIALES NO CONFIGURADAS")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return True

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = None
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") and getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(None, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, None, "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", None) or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr("_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", ) or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "XX_hostXX", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_HOST", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "XXXX") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(None, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, None, "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", None)
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr("host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", )
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "XXhostXX", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "HOST", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "XXlocalhostXX")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "LOCALHOST")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = None
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(None)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) and 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(None, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, None, 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", None) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr("_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", ) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "XX_portXX", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_PORT", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9201) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9201)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = None
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") and getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(None, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, None, "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", None) or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr("_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", ) or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "XX_usernameXX", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_USERNAME", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "XXXX") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(None, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, None, "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", None)
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr("username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", )
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "XXusernameXX", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "USERNAME", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "XXXX")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = None

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") and getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(None, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, None, "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", None) or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr("_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", ) or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "XX_passwordXX", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_PASSWORD", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "XXXX") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(None, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, None, "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", None)

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr("password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", )

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "XXpasswordXX", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "PASSWORD", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "XXXX")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = None
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") and getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(None, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, None, "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", None) or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr("_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", ) or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "XX_base_urlXX", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_BASE_URL", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "XXXX") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(None, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, None, "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", None)
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr("base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", )
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "XXbase_urlXX", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "BASE_URL", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "XXXX")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = None
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = None

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = None

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_107(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=None,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_108(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_109(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_110(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username or password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_111(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth(None, username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_112(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=None, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_113(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=None)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_114(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth(username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_115(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_116(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, )
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_117(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("XXBasicXX", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_118(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_119(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("BASIC", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_120(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = None
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_121(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") and getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_122(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(None, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_123(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, None, "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_124(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", None) or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_125(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr("_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_126(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_127(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", ) or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_128(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "XX_api_keyXX", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_129(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_API_KEY", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_130(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "XXXX") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_131(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(None, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_132(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, None, "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_133(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", None)
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_134(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr("api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_135(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_136(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", )
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_137(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "XXapi_keyXX", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_138(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "API_KEY", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_139(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "XXXX")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_140(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth(None, token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_141(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=None)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_142(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth(token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_143(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", )

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_144(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("XXApiKeyXX", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_145(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("apikey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_146(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("APIKEY", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_147(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = None
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_148(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get(None)
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_149(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("XX/XX")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_150(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code != 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_151(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 402:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_152(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error(None)
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_153(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("XXElasticConnector: credenciales invalidas (401)XX")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_154(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("elasticconnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_155(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ELASTICCONNECTOR: CREDENCIALES INVALIDAS (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_156(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return True
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_157(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_158(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(None)
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_159(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(None)

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_160(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = None
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_161(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = False
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_162(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation(None, "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_163(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", None)
        return True

    def xǁElasticConnectorǁconnect__mutmut_164(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_165(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", )
        return True

    def xǁElasticConnectorǁconnect__mutmut_166(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("XXconnectXX", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_167(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("CONNECT", "Conexion Elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_168(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "XXConexion Elasticsearch establecidaXX")
        return True

    def xǁElasticConnectorǁconnect__mutmut_169(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "conexion elasticsearch establecida")
        return True

    def xǁElasticConnectorǁconnect__mutmut_170(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "CONEXION ELASTICSEARCH ESTABLECIDA")
        return True

    def xǁElasticConnectorǁconnect__mutmut_171(self) -> bool:
        """Establece conexion con Elasticsearch."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("ElasticConnector: credenciales no configuradas")
            return False

        # Extract connection parameters from auth provider
        host = getattr(self._auth_provider, "_host", "") or getattr(self._auth_provider, "host", "localhost")
        port = int(getattr(self._auth_provider, "_port", 9200) or 9200)
        username = getattr(self._auth_provider, "_username", "") or getattr(self._auth_provider, "username", "")
        password = getattr(self._auth_provider, "_password", "") or getattr(self._auth_provider, "password", "")

        # Allow custom base URL
        custom_url = getattr(self._auth_provider, "_base_url", "") or getattr(self._auth_provider, "base_url", "")
        if custom_url:
            self._base_url = custom_url
        else:
            self._base_url = f"http://{host}:{port}"

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )

        # Set authentication
        if username and password:
            self._http.set_auth("Basic", username=username, password=password)
        else:
            # Try API key auth
            api_key = getattr(self._auth_provider, "_api_key", "") or getattr(self._auth_provider, "api_key", "")
            if api_key:
                self._http.set_auth("ApiKey", token=api_key)

        # Validate connection with a ping
        try:
            response = self._http.get("/")
            if response.status_code == 401:
                logger.error("ElasticConnector: credenciales invalidas (401)")
                return False
            if not response.ok:
                logger.warning(f"ElasticConnector: respuesta inesperada del cluster: {response.status_code}")
        except HTTPClientError as e:
            logger.warning(f"ElasticConnector: error validando conexion: {e}")

        self._connected = True
        self._log_operation("connect", "Conexion Elasticsearch establecida")
        return False

    @_mutmut_mutated(mutants_xǁElasticConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXindex_documentXX": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "INDEX_DOCUMENT": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "XXsearchXX": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "SEARCH": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "XXget_documentXX": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "GET_DOCUMENT": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "XXdelete_documentXX": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "DELETE_DOCUMENT": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "XXcreate_indexXX": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "CREATE_INDEX": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "XXlist_indicesXX": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "LIST_INDICES": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁElasticConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Elasticsearch.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "index_document": self._index_document,
            "search": self._search,
            "get_document": self._get_document,
            "delete_document": self._delete_document,
            "create_index": self._create_index,
            "list_indices": self._list_indices,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁElasticConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales de Elasticsearch esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁElasticConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales de Elasticsearch esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁElasticConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales de Elasticsearch esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁElasticConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales de Elasticsearch esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁElasticConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁElasticConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Elasticsearch."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁElasticConnectorǁ_index_document__mutmut)
    def _index_document(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = None
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get(None, "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", None)
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", )
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("XXindexXX", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("INDEX", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "XXXX")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = None
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get(None, {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", None)
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get({})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", )
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("XXdocumentXX", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("DOCUMENT", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = None
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get(None, "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", None)
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", )
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("XXidXX", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("ID", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "XXXX")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = None
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get(None, "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", None)
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", )
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("XXrefreshXX", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("REFRESH", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "XXfalseXX")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "FALSE")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = None
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get(None, "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", None)
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", )
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("XXpipelineXX", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("PIPELINE", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "XXXX")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index and not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"SUCCESS": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": True, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "XXerrorXX": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "ERROR": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "XXParametros requeridos: index, documentXX"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: INDEX, DOCUMENT"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation(None, f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", None)

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation(f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", )

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("XXindex_documentXX", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("INDEX_DOCUMENT", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = None
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"XXrefreshXX": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"REFRESH": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = None

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["XXpipelineXX"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["PIPELINE"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = None
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(None, json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=None, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=None)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, )
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = None

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(None, json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=None, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=None)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, )

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = None
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "XXsuccessXX": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "SUCCESS": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": False,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "XXindexXX": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "INDEX": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(None, index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", None),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", ),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("XX_indexXX", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_INDEX", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "XXidXX": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "ID": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get(None, doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", None),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get(doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", ),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("XX_idXX", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_ID", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "XXresultXX": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "RESULT": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get(None, "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", None),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", ),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("XXresultXX", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("RESULT", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "XXcreatedXX"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "CREATED"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "XXversionXX": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "VERSION": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get(None, 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", None),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get(1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", ),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("XX_versionXX", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_VERSION", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 2),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "XXseq_noXX": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "SEQ_NO": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get(None, None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get(None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", ),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("XX_seq_noXX", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_SEQ_NO", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "XXprimary_termXX": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "PRIMARY_TERM": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get(None, None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get(None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("XX_primary_termXX", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_PRIMARY_TERM", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁElasticConnectorǁ_index_document__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Indexa un documento en Elasticsearch.

        Args:
            params: Debe contener 'index', 'document' y opcionalmente 'id'
        """
        index = params.get("index", "")
        document = params.get("document", {})
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        pipeline = params.get("pipeline", "")
        if not index or not document:
            return {"success": False, "error": "Parametros requeridos: index, document"}
        self._log_operation("index_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            if pipeline:
                query_params["pipeline"] = pipeline

            if doc_id:
                # Use PUT for explicit ID
                response = self._http.put(f"/{index}/_doc/{doc_id}", json=document, params=query_params)
            else:
                # Use POST for auto-generated ID
                response = self._http.post(f"/{index}/_doc", json=document, params=query_params)

            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "created"),
                "version": data.get("_version", 1),
                "seq_no": data.get("_seq_no", None),
                "primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁElasticConnectorǁ_search__mutmut)
    def _search(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = None
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get(None, "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", None)
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", )
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("XXindexXX", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("INDEX", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "XXXX")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = None
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get(None, {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", None)
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get({})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", )
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("XXqueryXX", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("QUERY", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = None
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get(None, 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", None)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get(10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", )
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("XXsizeXX", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("SIZE", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 11)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = None
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get(None, 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", None)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get(0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", )
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("XXfromXX", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("FROM", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 1)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = None
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get(None)
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("XXsortXX")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("SORT")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = None
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get(None)
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("XX_sourceXX")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_SOURCE")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index and not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"SUCCESS": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": True, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "XXerrorXX": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "ERROR": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "XXParametros requeridos: index, queryXX"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: INDEX, QUERY"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation(None, f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", None)

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation(f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", )

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("XXsearchXX", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("SEARCH", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = None
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "XXqueryXX": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "QUERY": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "XXsizeXX": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "SIZE": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "XXfromXX": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "FROM": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = None
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["XXsortXX"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["SORT"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = None
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["XX_sourceXX"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_SOURCE"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get(None):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("XXaggsXX"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("AGGS"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = None
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["XXaggsXX"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["AGGS"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["XXaggsXX"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["AGGS"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get(None):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("XXhighlightXX"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("HIGHLIGHT"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = None

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["XXhighlightXX"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["HIGHLIGHT"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["XXhighlightXX"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["HIGHLIGHT"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = None
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(None)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else "XX,XX".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = None
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(None, json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", )
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = None
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = None
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get(None, {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", None)
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get({})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", )
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("XXhitsXX", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("HITS", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "XXsuccessXX": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "SUCCESS": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": False,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "XXhitsXX": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "HITS": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "XXtookXX": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "TOOK": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get(None, 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", None),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get(0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", ),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("XXtookXX", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("TOOK", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 1),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "XXtimed_outXX": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "TIMED_OUT": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get(None, False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", None),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get(False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", ),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("XXtimed_outXX", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("TIMED_OUT", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", True),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "XX_shardsXX": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_SHARDS": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get(None, {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", None),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get({}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", ),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("XX_shardsXX", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_SHARDS", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "XXaggregationsXX": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "AGGREGATIONS": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get(None, None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get(None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("XXaggregationsXX", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("AGGREGATIONS", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁElasticConnectorǁ_search__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Ejecuta una busqueda en Elasticsearch.

        Args:
            params: Debe contener 'index' y 'query' (dict con query DSL)
        """
        index = params.get("index", "")
        query = params.get("query", {})
        size = params.get("size", 10)
        from_ = params.get("from", 0)
        sort = params.get("sort")
        source = params.get("_source")
        if not index or not query:
            return {"success": False, "error": "Parametros requeridos: index, query"}
        self._log_operation("search", f"index={index}")

        try:
            body: dict[str, Any] = {
                "query": query,
                "size": size,
                "from": from_,
            }
            if sort:
                body["sort"] = sort
            if source is not None:
                body["_source"] = source
            if params.get("aggs"):
                body["aggs"] = params["aggs"]
            if params.get("highlight"):
                body["highlight"] = params["highlight"]

            # Support searching multiple indices
            index_path = index if isinstance(index, str) else ",".join(index)
            response = self._http.post(f"/{index_path}/_search", json=body)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            hits = data.get("hits", {})
            return {
                "success": True,
                "hits": hits,
                "took": data.get("took", 0),
                "timed_out": data.get("timed_out", False),
                "_shards": data.get("_shards", {}),
                "aggregations": data.get("aggregations", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁElasticConnectorǁ_get_document__mutmut)
    def _get_document(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = None
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get(None, "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", None)
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", )
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("XXindexXX", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("INDEX", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "XXXX")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = None
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get(None, "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", None)
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", )
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("XXidXX", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("ID", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "XXXX")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get(None)
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("XX_sourceXX")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_SOURCE")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = None
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get(None, "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", None)
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", )
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("XX_source_includesXX", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_SOURCE_INCLUDES", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "XXXX")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = None
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get(None, "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", None)
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", )
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("XX_source_excludesXX", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_SOURCE_EXCLUDES", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "XXXX")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index and not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"SUCCESS": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": True, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "XXerrorXX": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "ERROR": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "XXParametros requeridos: index, idXX"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: INDEX, ID"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation(None, f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", None)

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation(f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", )

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("XXget_documentXX", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("GET_DOCUMENT", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = None
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = None
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["XX_source_includesXX"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_SOURCE_INCLUDES"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = None

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["XX_source_excludesXX"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_SOURCE_EXCLUDES"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = None
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                None,
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code != 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 405:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"XXsuccessXX": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"SUCCESS": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": True, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "XXerrorXX": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "ERROR": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = None
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "XXsuccessXX": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "SUCCESS": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": False,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "XXindexXX": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "INDEX": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(None, index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", None),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", ),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("XX_indexXX", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_INDEX", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "XXidXX": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "ID": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get(None, doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", None),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get(doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", ),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("XX_idXX", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_ID", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "XXfoundXX": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "FOUND": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get(None, True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", None),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get(True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", ),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("XXfoundXX", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("FOUND", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", False),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "XX_sourceXX": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_SOURCE": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get(None, {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", None),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get({}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", ),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("XX_sourceXX", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_SOURCE", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "XX_versionXX": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_VERSION": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get(None, 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", None),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get(1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", ),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("XX_versionXX", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_VERSION", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 2),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "XX_seq_noXX": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_SEQ_NO": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get(None, None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get(None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", ),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("XX_seq_noXX", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_SEQ_NO", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "XX_primary_termXX": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_PRIMARY_TERM": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get(None, None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get(None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("XX_primary_termXX", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_PRIMARY_TERM", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁElasticConnectorǁ_get_document__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un documento por ID de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        params.get("_source")
        source_includes = params.get("_source_includes", "")
        source_excludes = params.get("_source_excludes", "")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("get_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {}
            if source_includes:
                query_params["_source_includes"] = source_includes
            if source_excludes:
                query_params["_source_excludes"] = source_excludes

            response = self._http.get(
                f"/{index}/_doc/{doc_id}",
                params=query_params if query_params else None,
            )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "found": data.get("found", True),
                "_source": data.get("_source", {}),
                "_version": data.get("_version", 1),
                "_seq_no": data.get("_seq_no", None),
                "_primary_term": data.get("_primary_term", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁElasticConnectorǁ_delete_document__mutmut)
    def _delete_document(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = None
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get(None, "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", None)
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", )
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("XXindexXX", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("INDEX", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "XXXX")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = None
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get(None, "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", None)
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", )
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("XXidXX", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("ID", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "XXXX")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = None
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get(None, "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", None)
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", )
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("XXrefreshXX", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("REFRESH", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "XXfalseXX")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "FALSE")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index and not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"SUCCESS": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": True, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "XXerrorXX": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "ERROR": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "XXParametros requeridos: index, idXX"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: INDEX, ID"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation(None, f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", None)

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation(f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", )

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("XXdelete_documentXX", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("DELETE_DOCUMENT", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = None
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"XXrefreshXX": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"REFRESH": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = None
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(None, params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=None)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", )
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code != 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 405:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"XXsuccessXX": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"SUCCESS": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": True, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "XXerrorXX": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "ERROR": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = None
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "XXsuccessXX": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "SUCCESS": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": False,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "XXindexXX": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "INDEX": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(None, index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", None),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", ),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("XX_indexXX", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_INDEX", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "XXidXX": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "ID": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get(None, doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", None),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get(doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", ),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("XX_idXX", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_ID", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "XXresultXX": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "RESULT": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get(None, "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", None),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", ),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("XXresultXX", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("RESULT", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "XXdeletedXX"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "DELETED"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "XX_versionXX": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_VERSION": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get(None, None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get(None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("XX_versionXX", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_VERSION", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁElasticConnectorǁ_delete_document__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un documento de Elasticsearch.

        Args:
            params: Debe contener 'index' y 'id'
        """
        index = params.get("index", "")
        doc_id = params.get("id", "")
        refresh = params.get("refresh", "false")
        if not index or not doc_id:
            return {"success": False, "error": "Parametros requeridos: index, id"}
        self._log_operation("delete_document", f"index={index}, id={doc_id}")

        try:
            query_params: dict[str, Any] = {"refresh": refresh}
            response = self._http.delete(f"/{index}/_doc/{doc_id}", params=query_params)
            if not response.ok:
                if response.status_code == 404:
                    return {"success": False, "error": f"Documento no encontrado: {index}/{doc_id}"}
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("_index", index),
                "id": data.get("_id", doc_id),
                "result": data.get("result", "deleted"),
                "_version": data.get("_version", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁElasticConnectorǁ_create_index__mutmut)
    def _create_index(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = None
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get(None, "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", None)
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", )
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("XXindexXX", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("INDEX", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "XXXX")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"XXsuccessXX": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"SUCCESS": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": True, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "XXerrorXX": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "ERROR": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "XXParametro requerido: indexXX"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "PARAMETRO REQUERIDO: INDEX"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation(None, f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", None)

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation(f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", )

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("XXcreate_indexXX", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("CREATE_INDEX", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = None
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get(None):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("XXmappingsXX"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("MAPPINGS"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = None
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["XXmappingsXX"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["MAPPINGS"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["XXmappingsXX"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["MAPPINGS"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get(None):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("XXsettingsXX"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("SETTINGS"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = None
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["XXsettingsXX"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["SETTINGS"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["XXsettingsXX"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["SETTINGS"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get(None):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("XXaliasesXX"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("ALIASES"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = None

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["XXaliasesXX"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["ALIASES"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["XXaliasesXX"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["ALIASES"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = None
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(None, json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", )
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = None
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "XXsuccessXX": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "SUCCESS": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": False,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "XXindexXX": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "INDEX": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(None, index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", None),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get(index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", ),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("XXindexXX", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("INDEX", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "XXacknowledgedXX": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "ACKNOWLEDGED": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get(None, False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", None),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get(False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", ),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("XXacknowledgedXX", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("ACKNOWLEDGED", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", True),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "XXshards_acknowledgedXX": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "SHARDS_ACKNOWLEDGED": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get(None, False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", None),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get(False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", ),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("XXshards_acknowledgedXX", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("SHARDS_ACKNOWLEDGED", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", True),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁElasticConnectorǁ_create_index__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un indice en Elasticsearch.

        Args:
            params: Debe contener 'index' y opcionalmente 'mappings', 'settings'
        """
        index = params.get("index", "")
        if not index:
            return {"success": False, "error": "Parametro requerido: index"}
        self._log_operation("create_index", f"index={index}")

        try:
            body: dict[str, Any] = {}
            if params.get("mappings"):
                body["mappings"] = params["mappings"]
            if params.get("settings"):
                body["settings"] = params["settings"]
            if params.get("aliases"):
                body["aliases"] = params["aliases"]

            response = self._http.put(f"/{index}", json=body if body else None)
            if not response.ok:
                return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
            data = response.json()
            return {
                "success": True,
                "index": data.get("index", index),
                "acknowledged": data.get("acknowledged", False),
                "shards_acknowledged": data.get("shards_acknowledged", False),
            }
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁElasticConnectorǁ_list_indices__mutmut)
    def _list_indices(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation(None)

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("XXlist_indicesXX")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("LIST_INDICES")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = None
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get(None, "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", None)
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", )
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("XXformatXX", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("FORMAT", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "XXjsonXX")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "JSON")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = None
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get(None, params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params=None)
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get(params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", )
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("XX/_cat/indicesXX", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_CAT/INDICES", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"XXformatXX": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"FORMAT": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = None
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get(None)
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("XX/_all/_mappingXX")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_ALL/_MAPPING")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"XXsuccessXX": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"SUCCESS": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": True, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "XXerrorXX": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "ERROR": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = None
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = None
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(None)
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"XXsuccessXX": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"SUCCESS": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": False, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "XXindicesXX": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "INDICES": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = None
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = None
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get(None, idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", None) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get(idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", ) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("XXindexXX", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("INDEX", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get(None, "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", None)) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", )) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("XXiXX", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("I", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "XXXX")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = None
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(None)
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = None

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"XXsuccessXX": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"SUCCESS": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": False, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "XXindicesXX": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "INDICES": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": True, "error": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "ERROR": str(e)}

    def xǁElasticConnectorǁ_list_indices__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los indices de Elasticsearch."""
        self._log_operation("list_indices")

        try:
            # Use the _cat/indices API for a clean list
            format_param = params.get("format", "json")
            response = self._http.get("/_cat/indices", params={"format": format_param})
            if not response.ok:
                # Fallback to _all/_mapping
                response = self._http.get("/_all/_mapping")
                if not response.ok:
                    return {"success": False, "error": f"HTTP {response.status_code}: {response.body}"}
                data = response.json()
                indices = list(data.keys())
                return {"success": True, "indices": indices}

            data = response.json()
            if isinstance(data, list):
                # _cat/indices returns a list of index info
                indices = [idx.get("index", idx.get("i", "")) for idx in data]
            elif isinstance(data, dict):
                indices = list(data.keys())
            else:
                indices = []

            return {"success": True, "indices": indices}
        except HTTPClientError as e:
            return {"success": False, "error": str(None)}

mutants_xǁElasticConnectorǁ__init____mutmut['_mutmut_orig'] = ElasticConnector.xǁElasticConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ__init____mutmut['xǁElasticConnectorǁ__init____mutmut_1'] = ElasticConnector.xǁElasticConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ__init____mutmut['xǁElasticConnectorǁ__init____mutmut_2'] = ElasticConnector.xǁElasticConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ__init____mutmut['xǁElasticConnectorǁ__init____mutmut_3'] = ElasticConnector.xǁElasticConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ__init____mutmut['xǁElasticConnectorǁ__init____mutmut_4'] = ElasticConnector.xǁElasticConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁElasticConnector�
# type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_34'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_34 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_35'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_35 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_36'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_36 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_37'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_37 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_38'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_38 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_39'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_39 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_40'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_40 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_41'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_41 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_42'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_42 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_43'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_43 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_44'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_44 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_45'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_45 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_46'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_46 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_47'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_47 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_48'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_48 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_49'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_49 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_50'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_50 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_51'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_51 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_52'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_52 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_53'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_53 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_54'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_54 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_55'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_55 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_56'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_56 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_57'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_57 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_58'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_58 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_59'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_59 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_60'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_60 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_61'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_61 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_62'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_62 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_63'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_63 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_64'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_64 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_65'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_65 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_66'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_66 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_67'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_67 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_68'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_68 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_69'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_69 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_70'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_70 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_71'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_71 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_72'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_72 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_73'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_73 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_74'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_74 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_75'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_75 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_76'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_76 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_77'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_77 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_78'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_78 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_79'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_79 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_80'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_80 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_81'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_81 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_82'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_82 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_83'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_83 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_84'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_84 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_85'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_85 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_86'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_86 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_87'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_87 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_88'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_88 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_89'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_89 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_90'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_90 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_91'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_91 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_92'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_92 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_93'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_93 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_94'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_94 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_create_index__mutmut['xǁElasticConnectorǁ_create_index__mutmut_95'] = ElasticConnector.xǁElasticConnectorǁ_create_index__mutmut_95 # type: ignore # mutmut generated

mutants_xǁElasticConnectorǁ_list_indices__mutmut['_mutmut_orig'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_orig # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_1'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_1 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_2'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_2 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_3'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_3 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_4'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_4 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_5'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_5 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_6'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_6 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_7'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_7 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_8'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_8 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_9'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_9 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_10'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_10 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_11'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_11 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_12'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_12 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_13'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_13 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_14'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_14 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_15'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_15 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_16'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_16 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_17'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_17 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_18'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_18 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_19'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_19 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_20'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_20 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_21'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_21 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_22'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_22 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_23'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_23 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_24'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_24 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_25'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_25 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_26'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_26 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_27'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_27 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_28'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_28 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_29'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_29 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_30'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_30 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_31'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_31 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_32'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_32 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_33'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_33 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_34'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_34 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_35'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_35 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_36'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_36 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_37'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_37 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_38'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_38 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_39'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_39 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_40'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_40 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_41'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_41 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_42'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_42 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_43'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_43 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_44'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_44 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_45'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_45 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_46'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_46 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_47'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_47 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_48'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_48 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_49'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_49 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_50'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_50 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_51'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_51 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_52'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_52 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_53'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_53 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_54'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_54 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_55'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_55 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_56'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_56 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_57'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_57 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_58'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_58 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_59'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_59 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_60'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_60 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_61'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_61 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_62'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_62 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_63'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_63 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_64'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_64 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_65'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_65 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_66'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_66 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_67'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_67 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_68'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_68 # type: ignore # mutmut generated
mutants_xǁElasticConnectorǁ_list_indices__mutmut['xǁElasticConnectorǁ_list_indices__mutmut_69'] = ElasticConnector.xǁElasticConnectorǁ_list_indices__mutmut_69 # type: ignore # mutmut generated


ELASTIC_SCHEMA = ConnectorSchema(
    name="elastic",
    version="1.0.0",
    description="Indexa documentos y ejecuta busquedas en Elasticsearch",
    category="databases",
    icon="search",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="index_document", description="Indexa un documento", category="write"),
        ActionDefinition(name="search", description="Ejecuta una busqueda", category="read"),
        ActionDefinition(name="get_document", description="Obtiene un documento", category="read"),
        ActionDefinition(name="delete_document", description="Elimina un documento", category="delete"),
        ActionDefinition(name="create_index", description="Crea un indice", category="write"),
        ActionDefinition(name="list_indices", description="Lista indices", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="basic", required_fields=["host", "username", "password"], description="Credenciales Elasticsearch")
    ],
)
