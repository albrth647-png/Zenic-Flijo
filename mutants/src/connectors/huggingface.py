"""
Conector HuggingFace — Inferencia de Modelos via HuggingFace API
===================================================================

Permite ejecutar inferencia en modelos de NLP, vision, audio
y generacion via la Inference API de HuggingFace.
"""

from __future__ import annotations

import base64
from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁHuggingfaceConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut: MutantDict = {}  # type: ignore
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut: MutantDict = {}  # type: ignore


class HuggingfaceConnector(BaseConnector):
    """Conector para HuggingFace: inferencia de modelos de IA."""

    name = "huggingface"
    version = "1.0.0"
    description = "Ejecuta inferencia en modelos de NLP, vision y audio via HuggingFace"
    category = "ai_data"
    icon = "cpu"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-inference.huggingface.co/models"
        self._http: HttpClient | None = None

    def xǁHuggingfaceConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-inference.huggingface.co/models"
        self._http: HttpClient | None = None

    def xǁHuggingfaceConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁHuggingfaceConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://api-inference.huggingface.co/modelsXX"
        self._http: HttpClient | None = None

    def xǁHuggingfaceConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://API-INFERENCE.HUGGINGFACE.CO/MODELS"
        self._http: HttpClient | None = None

    def xǁHuggingfaceConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-inference.huggingface.co/models"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut)
    def _get_api_token(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_orig(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_1(self) -> str:
        """Extract API token from the auth provider."""
        if self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_2(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return "XXXX"
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_3(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = None
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_4(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(None, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_5(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, None, "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_6(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", None)
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_7(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr("_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_8(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_9(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", )
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_10(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "XX_api_keyXX", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_11(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_API_KEY", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_12(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "XXXX")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_13(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = None
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_14(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"XXheadersXX": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_15(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"HEADERS": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_16(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "XXparamsXX": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_17(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "PARAMS": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_18(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(None)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_19(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = None
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_20(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get(None, {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_21(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", None)
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_22(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get({})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_23(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", )
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_24(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("XXheadersXX", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_25(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("HEADERS", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_26(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = None
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_27(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get(None, headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_28(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", None)
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_29(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get(headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_30(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", )
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_31(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("XXX-API-KeyXX", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_32(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("x-api-key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_33(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-KEY", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_34(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace(None, ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_35(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", None))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_36(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace(""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_37(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_38(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get(None, "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_39(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", None).replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_40(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_41(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", ).replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_42(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("XXAuthorizationXX", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_43(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("authorization", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_44(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("AUTHORIZATION", "").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_45(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "XXXX").replace("Bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_46(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("XXBearer XX", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_47(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("bearer ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_48(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("BEARER ", ""))
        return api_token

    def xǁHuggingfaceConnectorǁ_get_api_token__mutmut_49(self) -> str:
        """Extract API token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth stores token in _api_key)
        api_token = getattr(self._auth_provider, "_api_key", "")
        if api_token:
            return api_token
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_token = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", "XXXX"))
        return api_token

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXHuggingfaceConnector: API Token no configuradoXX")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("huggingfaceconnector: api token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HUGGINGFACECONNECTOR: API TOKEN NO CONFIGURADO")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return True

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = None
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error(None)
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("XXHuggingfaceConnector: No se pudo extraer el API Token del auth providerXX")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("huggingfaceconnector: no se pudo extraer el api token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HUGGINGFACECONNECTOR: NO SE PUDO EXTRAER EL API TOKEN DEL AUTH PROVIDER")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return True

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = None
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=None,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(None, token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=None)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", )

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("XXBearerXX", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("BEARER", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = None
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = False
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation(None, "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", None)
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", )
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("XXconnectXX", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("CONNECT", "API Token configurado, HttpClient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "XXAPI Token configurado, HttpClient inicializadoXX")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "api token configurado, httpclient inicializado")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API TOKEN CONFIGURADO, HTTPCLIENT INICIALIZADO")
        return True

    def xǁHuggingfaceConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la Inference API de HuggingFace."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("HuggingfaceConnector: API Token no configurado")
            return False

        api_token = self._get_api_token()
        if not api_token:
            logger.error("HuggingfaceConnector: No se pudo extraer el API Token del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_token)

        self._connected = True
        self._log_operation("connect", "API Token configurado, HttpClient inicializado")
        return False

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXtext_generationXX": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "TEXT_GENERATION": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "XXtext_classificationXX": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "TEXT_CLASSIFICATION": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "XXsummarizationXX": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "SUMMARIZATION": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "XXtranslationXX": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "TRANSLATION": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "XXimage_classificationXX": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "IMAGE_CLASSIFICATION": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "XXspeech_recognitionXX": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "SPEECH_RECOGNITION": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁHuggingfaceConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector HuggingFace.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "text_generation": self._text_generation,
            "text_classification": self._text_classification,
            "summarization": self._summarization,
            "translation": self._translation,
            "image_classification": self._image_classification,
            "speech_recognition": self._speech_recognition,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el API Token de HuggingFace este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁHuggingfaceConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el API Token de HuggingFace este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁHuggingfaceConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el API Token de HuggingFace este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁHuggingfaceConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el API Token de HuggingFace este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁHuggingfaceConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con HuggingFace."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut)
    def _text_generation(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = None
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get(None, "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", None)
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", )
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("XXmodel_idXX", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("MODEL_ID", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "XXXX")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = None
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get(None, "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", None)
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", )
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("XXinputsXX", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("INPUTS", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "XXXX")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id and not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"SUCCESS": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": True, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "XXerrorXX": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "ERROR": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "XXParametros requeridos: model_id, inputsXX"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: MODEL_ID, INPUTS"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(None, f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", None)

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", )

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("XXtext_generationXX", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("TEXT_GENERATION", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = None
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"XXinputsXX": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"INPUTS": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "XXparametersXX" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "PARAMETERS" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" not in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = None
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["XXparametersXX"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["PARAMETERS"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["XXparametersXX"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["PARAMETERS"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "XXoptionsXX" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "OPTIONS" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" not in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = None

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["XXoptionsXX"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["OPTIONS"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["XXoptionsXX"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["OPTIONS"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(None, json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=None, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=121)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "XXdataXX": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "DATA": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "XXmodel_idXX": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "MODEL_ID": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁHuggingfaceConnectorǁ_text_generation__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs', opcionalmente 'parameters' (max_new_tokens, temperature, etc.)
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_generation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]
            if "options" in params:
                body["options"] = params["options"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_generation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_generation: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut)
    def _text_classification(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = None
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get(None, "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", None)
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", )
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("XXmodel_idXX", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("MODEL_ID", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "XXXX")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = None
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get(None, "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", None)
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", )
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("XXinputsXX", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("INPUTS", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "XXXX")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id and not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"SUCCESS": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": True, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "XXerrorXX": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "ERROR": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "XXParametros requeridos: model_id, inputsXX"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: MODEL_ID, INPUTS"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(None, f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", None)

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", )

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("XXtext_classificationXX", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("TEXT_CLASSIFICATION", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = None
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"XXinputsXX": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"INPUTS": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "XXparametersXX" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "PARAMETERS" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" not in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = None

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["XXparametersXX"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["PARAMETERS"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["XXparametersXX"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["PARAMETERS"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(None, json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=None, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=61)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "XXdataXX": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "DATA": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "XXmodel_idXX": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "MODEL_ID": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁHuggingfaceConnectorǁ_text_classification__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("text_classification", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.text_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.text_classification: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut)
    def _summarization(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = None
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get(None, "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", None)
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", )
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("XXmodel_idXX", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("MODEL_ID", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "XXXX")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = None
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get(None, "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", None)
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", )
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("XXinputsXX", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("INPUTS", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "XXXX")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id and not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"SUCCESS": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": True, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "XXerrorXX": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "ERROR": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "XXParametros requeridos: model_id, inputsXX"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: MODEL_ID, INPUTS"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(None, f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", None)

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", )

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("XXsummarizationXX", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("SUMMARIZATION", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = None
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"XXinputsXX": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"INPUTS": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "XXparametersXX" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "PARAMETERS" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" not in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = None

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["XXparametersXX"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["PARAMETERS"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["XXparametersXX"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["PARAMETERS"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(None, json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=None, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=121)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "XXdataXX": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "DATA": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "XXmodel_idXX": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "MODEL_ID": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁHuggingfaceConnectorǁ_summarization__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Resume un texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("summarization", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.summarization: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.summarization: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ_translation__mutmut)
    def _translation(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = None
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get(None, "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", None)
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", )
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("XXmodel_idXX", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("MODEL_ID", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "XXXX")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = None
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get(None, "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", None)
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", )
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("XXinputsXX", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("INPUTS", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "XXXX")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id and not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"SUCCESS": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": True, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "XXerrorXX": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "ERROR": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "XXParametros requeridos: model_id, inputsXX"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: MODEL_ID, INPUTS"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(None, f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", None)

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation(f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", )

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("XXtranslationXX", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("TRANSLATION", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = None
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"XXinputsXX": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"INPUTS": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "XXparametersXX" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "PARAMETERS" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" not in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = None

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["XXparametersXX"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["PARAMETERS"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["XXparametersXX"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["PARAMETERS"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(None, json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=None, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=121)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "XXdataXX": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "DATA": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "XXmodel_idXX": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "MODEL_ID": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁHuggingfaceConnectorǁ_translation__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Traduce texto usando un modelo de HuggingFace.

        Args:
            params: Debe contener 'model_id' y 'inputs'
        """
        model_id = params.get("model_id", "")
        inputs = params.get("inputs", "")
        if not model_id or not inputs:
            return {"success": False, "error": "Parametros requeridos: model_id, inputs"}

        self._log_operation("translation", f"model={model_id}")

        try:
            body: dict[str, Any] = {"inputs": inputs}
            if "parameters" in params:
                body["parameters"] = params["parameters"]

            response = self._http.post(f"/{model_id}", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"HuggingFace API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.translation: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.translation: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut)
    def _image_classification(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = None
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get(None, "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", None)
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", )
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("XXmodel_idXX", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("MODEL_ID", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "XXXX")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = None
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get(None, "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", None)
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", )
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("XXimageXX", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("IMAGE", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "XXXX")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id and not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"SUCCESS": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": True, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "XXerrorXX": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "ERROR": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "XXParametros requeridos: model_id, imageXX"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: MODEL_ID, IMAGE"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation(None, f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", None)

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation(f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", )

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("XXimage_classificationXX", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("IMAGE_CLASSIFICATION", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = None
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = None

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") and image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith(None) or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("XXhttp://XX") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("HTTP://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith(None):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("XXhttps://XX"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("HTTPS://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = None
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(None, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=None)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, )
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=31)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"XXsuccessXX": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"SUCCESS": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": True, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "XXerrorXX": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "ERROR": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = None
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = None
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(None)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"XXsuccessXX": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"SUCCESS": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": True, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "XXerrorXX": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "ERROR": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "XXEl parametro 'image' debe ser una URL o base64XX"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "el parametro 'image' debe ser una url o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "EL PARAMETRO 'IMAGE' DEBE SER UNA URL O BASE64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = None

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "XXAuthorizationXX": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "AUTHORIZATION": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "XXContent-TypeXX": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "content-type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "CONTENT-TYPE": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "XXimage/jpegXX",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "IMAGE/JPEG",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = None

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(None, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=None, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=None, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=None)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=61)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 201 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 < resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code <= 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 301:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"XXsuccessXX": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"SUCCESS": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": True, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "XXerrorXX": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "ERROR": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = None
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"XXsuccessXX": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"SUCCESS": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": False, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "XXdataXX": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "DATA": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "XXmodel_idXX": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "MODEL_ID": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁHuggingfaceConnectorǁ_image_classification__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Clasifica una imagen usando un modelo de vision de HuggingFace.

        Uses the requests library directly since the HuggingFace image
        classification API expects binary image data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'image' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        image = params.get("image", "")
        if not model_id or not image:
            return {"success": False, "error": "Parametros requeridos: model_id, image"}

        self._log_operation("image_classification", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if image is base64 or a URL
            if image.startswith("http://") or image.startswith("https://"):
                # Download the image first
                img_response = req_lib.get(image, timeout=30)
                if not img_response.ok:
                    return {"success": False, "error": f"Failed to download image: {img_response.status_code}"}
                image_bytes = img_response.content
            else:
                # Assume base64-encoded image
                try:
                    image_bytes = base64.b64decode(image)
                except Exception:
                    return {"success": False, "error": "El parametro 'image' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "image/jpeg",
            }

            resp = req_lib.post(url, headers=headers, data=image_bytes, timeout=60)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.image_classification: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.image_classification: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut)
    def _speech_recognition(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = None
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get(None, "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", None)
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", )
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("XXmodel_idXX", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("MODEL_ID", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "XXXX")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = None
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get(None, "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", None)
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", )
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("XXaudioXX", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("AUDIO", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "XXXX")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id and not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"SUCCESS": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": True, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "XXerrorXX": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "ERROR": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "XXParametros requeridos: model_id, audioXX"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: MODEL_ID, AUDIO"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation(None, f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", None)

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation(f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", )

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("XXspeech_recognitionXX", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("SPEECH_RECOGNITION", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = None
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = None

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") and audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith(None) or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("XXhttp://XX") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("HTTP://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith(None):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("XXhttps://XX"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("HTTPS://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = None
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(None, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=None)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, )
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=31)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"XXsuccessXX": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"SUCCESS": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": True, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "XXerrorXX": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "ERROR": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = None
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = None
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(None)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"XXsuccessXX": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"SUCCESS": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": True, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "XXerrorXX": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "ERROR": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "XXEl parametro 'audio' debe ser una URL o base64XX"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "el parametro 'audio' debe ser una url o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "EL PARAMETRO 'AUDIO' DEBE SER UNA URL O BASE64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = None

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "XXAuthorizationXX": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "AUTHORIZATION": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "XXContent-TypeXX": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "content-type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "CONTENT-TYPE": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "XXaudio/wavXX",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "AUDIO/WAV",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = None

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(None, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=None, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=None, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=None)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=121)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 201 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 < resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code <= 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 301:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"XXsuccessXX": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"SUCCESS": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": True, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "XXerrorXX": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "ERROR": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = None
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"XXsuccessXX": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"SUCCESS": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": False, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "XXdataXX": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "DATA": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "XXmodel_idXX": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "MODEL_ID": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando un modelo de HuggingFace.

        Uses the requests library directly since the HuggingFace speech
        recognition API expects binary audio data, not JSON.

        Args:
            params: Debe contener 'model_id' y 'audio' (base64 o URL)
        """
        model_id = params.get("model_id", "")
        audio = params.get("audio", "")
        if not model_id or not audio:
            return {"success": False, "error": "Parametros requeridos: model_id, audio"}

        self._log_operation("speech_recognition", f"model={model_id}")

        try:
            import requests as req_lib

            api_token = self._get_api_token()
            url = f"{self._base_url}/{model_id}"

            # Determine if audio is base64 or a URL
            if audio.startswith("http://") or audio.startswith("https://"):
                # Download the audio first
                audio_response = req_lib.get(audio, timeout=30)
                if not audio_response.ok:
                    return {"success": False, "error": f"Failed to download audio: {audio_response.status_code}"}
                audio_bytes = audio_response.content
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(audio)
                except Exception:
                    return {"success": False, "error": "El parametro 'audio' debe ser una URL o base64"}

            headers = {
                "Authorization": f"Bearer {api_token}",
                "Content-Type": "audio/wav",
            }

            resp = req_lib.post(url, headers=headers, data=audio_bytes, timeout=120)

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"HuggingFace API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, "data": data, "model_id": model_id}

        except HTTPClientError as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"HuggingfaceConnector.speech_recognition: error: {e}")
            return {"success": False, "error": str(None)}

mutants_xǁHuggingfaceConnectorǁ__init____mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ__init____mutmut['xǁHuggingfaceConnectorǁ__init____mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ__init____mutmut['xǁHuggingfaceConnectorǁ__init____mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ__init____mutmut['xǁHuggingfaceConnectorǁ__init____mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ__init____mutmut['xǁHuggingfaceConnectorǁ__init____mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_39 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_40'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_40 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_41'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_41 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_42'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_42 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_43'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_43 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_44'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_44 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_45'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_45 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_46'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_46 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_47'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_47 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_48'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_48 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_get_api_token__mutmut['xǁHuggingfaceConnectorǁ_get_api_token__mutmut_49'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_get_api_token__mutmut_49 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁconnect__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁconnect__mutmut['xǁHuggingfaceConnectorǁconnect__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁconnect__mutmut_39 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁexecute__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁexecute__mutmut['xǁHuggingfaceConnectorǁexecute__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁexecute__mutmut_22 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁvalidate__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁvalidate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁvalidate__mutmut['xǁHuggingfaceConnectorǁvalidate__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁvalidate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁvalidate__mutmut['xǁHuggingfaceConnectorǁvalidate__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁvalidate__mutmut_2 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['xǁHuggingfaceConnectorǁdisconnect__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['xǁHuggingfaceConnectorǁdisconnect__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['xǁHuggingfaceConnectorǁdisconnect__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['xǁHuggingfaceConnectorǁdisconnect__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['xǁHuggingfaceConnectorǁdisconnect__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['xǁHuggingfaceConnectorǁdisconnect__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁdisconnect__mutmut['xǁHuggingfaceConnectorǁdisconnect__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁdisconnect__mutmut_7 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_39 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_40'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_40 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_41'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_41 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_42'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_42 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_43'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_43 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_44'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_44 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_45'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_45 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_46'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_46 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_47'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_47 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_48'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_48 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_49'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_49 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_50'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_50 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_51'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_51 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_52'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_52 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_53'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_53 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_54'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_54 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_55'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_55 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_56'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_56 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_57'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_57 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_58'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_58 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_59'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_59 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_60'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_60 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_61'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_61 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_62'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_62 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_63'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_63 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_64'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_64 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_65'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_65 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_66'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_66 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_67'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_67 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_68'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_68 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_69'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_69 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_70'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_70 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_71'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_71 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_72'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_72 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_73'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_73 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_74'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_74 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_75'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_75 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_76'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_76 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_77'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_77 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_78'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_78 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_79'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_79 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_80'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_80 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_81'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_81 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_82'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_82 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_83'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_83 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_84'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_84 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_85'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_85 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_86'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_86 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_87'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_87 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_88'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_88 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_89'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_89 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_generation__mutmut['xǁHuggingfaceConnectorǁ_text_generation__mutmut_90'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_generation__mutmut_90 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_39 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_40'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_40 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_41'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_41 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_42'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_42 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_43'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_43 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_44'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_44 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_45'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_45 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_46'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_46 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_47'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_47 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_48'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_48 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_49'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_49 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_50'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_50 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_51'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_51 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_52'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_52 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_53'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_53 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_54'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_54 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_55'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_55 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_56'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_56 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_57'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_57 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_58'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_58 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_59'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_59 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_60'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_60 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_61'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_61 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_62'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_62 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_63'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_63 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_64'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_64 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_65'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_65 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_66'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_66 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_67'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_67 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_68'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_68 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_69'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_69 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_70'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_70 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_71'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_71 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_72'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_72 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_73'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_73 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_74'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_74 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_75'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_75 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_76'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_76 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_77'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_77 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_78'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_78 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_79'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_79 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_80'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_80 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_81'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_81 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_text_classification__mutmut['xǁHuggingfaceConnectorǁ_text_classification__mutmut_82'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_text_classification__mutmut_82 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_39 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_40'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_40 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_41'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_41 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_42'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_42 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_43'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_43 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_44'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_44 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_45'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_45 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_46'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_46 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_47'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_47 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_48'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_48 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_49'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_49 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_50'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_50 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_51'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_51 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_52'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_52 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_53'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_53 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_54'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_54 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_55'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_55 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_56'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_56 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_57'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_57 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_58'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_58 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_59'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_59 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_60'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_60 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_61'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_61 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_62'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_62 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_63'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_63 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_64'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_64 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_65'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_65 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_66'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_66 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_67'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_67 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_68'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_68 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_69'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_69 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_70'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_70 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_71'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_71 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_72'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_72 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_73'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_73 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_74'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_74 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_75'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_75 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_76'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_76 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_77'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_77 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_78'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_78 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_79'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_79 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_80'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_80 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_81'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_81 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_summarization__mutmut['xǁHuggingfaceConnectorǁ_summarization__mutmut_82'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_summarization__mutmut_82 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_39 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_40'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_40 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_41'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_41 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_42'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_42 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_43'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_43 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_44'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_44 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_45'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_45 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_46'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_46 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_47'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_47 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_48'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_48 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_49'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_49 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_50'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_50 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_51'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_51 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_52'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_52 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_53'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_53 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_54'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_54 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_55'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_55 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_56'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_56 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_57'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_57 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_58'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_58 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_59'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_59 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_60'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_60 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_61'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_61 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_62'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_62 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_63'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_63 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_64'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_64 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_65'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_65 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_66'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_66 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_67'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_67 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_68'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_68 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_69'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_69 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_70'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_70 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_71'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_71 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_72'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_72 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_73'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_73 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_74'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_74 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_75'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_75 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_76'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_76 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_77'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_77 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_78'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_78 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_79'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_79 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_80'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_80 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_81'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_81 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_translation__mutmut['xǁHuggingfaceConnectorǁ_translation__mutmut_82'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_translation__mutmut_82 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_39 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_40'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_40 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_41'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_41 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_42'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_42 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_43'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_43 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_44'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_44 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_45'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_45 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_46'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_46 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_47'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_47 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_48'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_48 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_49'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_49 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_50'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_50 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_51'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_51 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_52'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_52 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_53'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_53 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_54'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_54 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_55'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_55 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_56'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_56 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_57'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_57 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_58'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_58 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_59'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_59 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_60'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_60 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_61'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_61 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_62'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_62 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_63'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_63 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_64'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_64 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_65'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_65 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_66'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_66 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_67'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_67 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_68'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_68 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_69'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_69 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_70'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_70 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_71'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_71 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_72'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_72 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_73'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_73 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_74'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_74 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_75'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_75 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_76'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_76 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_77'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_77 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_78'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_78 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_79'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_79 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_80'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_80 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_81'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_81 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_82'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_82 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_83'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_83 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_84'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_84 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_85'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_85 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_86'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_86 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_87'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_87 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_88'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_88 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_89'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_89 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_90'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_90 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_91'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_91 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_92'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_92 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_93'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_93 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_94'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_94 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_95'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_95 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_96'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_96 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_97'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_97 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_98'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_98 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_99'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_99 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_100'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_100 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_101'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_101 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_102'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_102 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_103'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_103 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_104'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_104 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_105'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_105 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_106'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_106 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_107'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_107 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_108'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_108 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_109'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_109 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_110'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_110 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_111'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_111 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_112'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_112 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_113'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_113 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_114'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_114 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_115'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_115 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_image_classification__mutmut['xǁHuggingfaceConnectorǁ_image_classification__mutmut_116'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_image_classification__mutmut_116 # type: ignore # mutmut generated

mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['_mutmut_orig'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_orig # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_1'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_1 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_2'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_2 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_3'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_3 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_4'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_4 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_5'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_5 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_6'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_6 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_7'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_7 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_8'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_8 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_9'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_9 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_10'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_10 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_11'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_11 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_12'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_12 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_13'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_13 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_14'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_14 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_15'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_15 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_16'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_16 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_17'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_17 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_18'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_18 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_19'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_19 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_20'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_20 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_21'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_21 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_22'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_22 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_23'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_23 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_24'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_24 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_25'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_25 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_26'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_26 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_27'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_27 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_28'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_28 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_29'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_29 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_30'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_30 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_31'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_31 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_32'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_32 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_33'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_33 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_34'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_34 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_35'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_35 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_36'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_36 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_37'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_37 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_38'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_38 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_39'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_39 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_40'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_40 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_41'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_41 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_42'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_42 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_43'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_43 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_44'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_44 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_45'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_45 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_46'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_46 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_47'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_47 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_48'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_48 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_49'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_49 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_50'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_50 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_51'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_51 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_52'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_52 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_53'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_53 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_54'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_54 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_55'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_55 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_56'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_56 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_57'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_57 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_58'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_58 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_59'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_59 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_60'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_60 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_61'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_61 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_62'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_62 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_63'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_63 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_64'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_64 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_65'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_65 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_66'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_66 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_67'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_67 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_68'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_68 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_69'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_69 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_70'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_70 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_71'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_71 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_72'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_72 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_73'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_73 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_74'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_74 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_75'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_75 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_76'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_76 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_77'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_77 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_78'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_78 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_79'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_79 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_80'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_80 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_81'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_81 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_82'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_82 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_83'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_83 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_84'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_84 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_85'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_85 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_86'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_86 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_87'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_87 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_88'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_88 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_89'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_89 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_90'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_90 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_91'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_91 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_92'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_92 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_93'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_93 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_94'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_94 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_95'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_95 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_96'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_96 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_97'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_97 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_98'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_98 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_99'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_99 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_100'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_100 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_101'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_101 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_102'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_102 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_103'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_103 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_104'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_104 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_105'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_105 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_106'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_106 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_107'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_107 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_108'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_108 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_109'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_109 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_110'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_110 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_111'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_111 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_112'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_112 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_113'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_113 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_114'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_114 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_115'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_115 # type: ignore # mutmut generated
mutants_xǁHuggingfaceConnectorǁ_speech_recognition__mutmut['xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_116'] = HuggingfaceConnector.xǁHuggingfaceConnectorǁ_speech_recognition__mutmut_116 # type: ignore # mutmut generated


HUGGINGFACE_SCHEMA = ConnectorSchema(
    name="huggingface",
    version="1.0.0",
    description="Ejecuta inferencia en modelos de NLP, vision y audio via HuggingFace",
    category="ai_data",
    icon="cpu",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="text_generation", description="Genera texto", category="write"),
        ActionDefinition(name="text_classification", description="Clasifica texto", category="read"),
        ActionDefinition(name="summarization", description="Resume texto", category="write"),
        ActionDefinition(name="translation", description="Traduce texto", category="write"),
        ActionDefinition(name="image_classification", description="Clasifica imagen", category="read"),
        ActionDefinition(name="speech_recognition", description="Transcribe audio", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="api_key", required_fields=["api_token"], description="HuggingFace API Token")
    ],
)
