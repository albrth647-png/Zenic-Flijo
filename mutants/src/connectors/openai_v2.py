"""
Conector OpenAI v2 — GPT y DALL-E via OpenAI API (Enhanced)
===============================================================

Permite generar texto con GPT-4/GPT-3.5, imagenes con DALL-E,
embeddings y audio con Whisper/TTS via la API de OpenAI.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁOpenaiV2Connectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁ_chat_completion__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁ_create_image__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁ_create_embedding__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut: MutantDict = {}  # type: ignore


class OpenaiV2Connector(BaseConnector):
    """Conector mejorado para OpenAI: GPT, DALL-E, Embeddings, Whisper y TTS."""

    name = "openai_v2"
    version = "2.0.0"
    description = "Genera texto con GPT, imagenes con DALL-E, embeddings y audio via OpenAI"
    category = "ai_data"
    icon = "sparkles"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.openai.com/v1"
        self._http: HttpClient | None = None

    def xǁOpenaiV2Connectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.openai.com/v1"
        self._http: HttpClient | None = None

    def xǁOpenaiV2Connectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁOpenaiV2Connectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://api.openai.com/v1XX"
        self._http: HttpClient | None = None

    def xǁOpenaiV2Connectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://API.OPENAI.COM/V1"
        self._http: HttpClient | None = None

    def xǁOpenaiV2Connectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api.openai.com/v1"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut)
    def _get_api_key(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_orig(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_1(self) -> str:
        """Extract API key from the auth provider."""
        if self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_2(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return "XXXX"
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_3(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = None
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_4(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(None, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_5(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, None, "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_6(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", None)
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_7(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr("_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_8(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_9(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", )
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_10(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "XX_api_keyXX", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_11(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_API_KEY", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_12(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "XXXX")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_13(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = None
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_14(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"XXheadersXX": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_15(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"HEADERS": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_16(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "XXparamsXX": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_17(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "PARAMS": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_18(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(None)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_19(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = None
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_20(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get(None, {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_21(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", None)
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_22(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get({})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_23(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", )
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_24(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("XXheadersXX", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_25(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("HEADERS", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_26(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = None
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_27(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get(None, headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_28(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", None)
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_29(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get(headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_30(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", )
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_31(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("XXX-API-KeyXX", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_32(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("x-api-key", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_33(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-KEY", headers.get("Authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_34(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace(None, ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_35(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", None))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_36(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace(""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_37(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", ))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_38(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get(None, "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_39(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", None).replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_40(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_41(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", ).replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_42(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("XXAuthorizationXX", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_43(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("authorization", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_44(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("AUTHORIZATION", "").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_45(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "XXXX").replace("Bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_46(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("XXBearer XX", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_47(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("bearer ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_48(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("BEARER ", ""))
        return api_key

    def xǁOpenaiV2Connectorǁ_get_api_key__mutmut_49(self) -> str:
        """Extract API key from the auth provider."""
        if not self._auth_provider:
            return ""
        # Try to access _api_key directly (APIKeyAuth)
        api_key = getattr(self._auth_provider, "_api_key", "")
        if api_key:
            return api_key
        # Fallback: use apply_auth and extract from headers
        auth_request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(auth_request)
        headers = auth_request.get("headers", {})
        api_key = headers.get("X-API-Key", headers.get("Authorization", "").replace("Bearer ", "XXXX"))
        return api_key

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXOpenaiV2Connector: API Key no configuradaXX")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("openaiv2connector: api key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OPENAIV2CONNECTOR: API KEY NO CONFIGURADA")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return True

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = None
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error(None)
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("XXOpenaiV2Connector: No se pudo extraer la API Key del auth providerXX")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("openaiv2connector: no se pudo extraer la api key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OPENAIV2CONNECTOR: NO SE PUDO EXTRAER LA API KEY DEL AUTH PROVIDER")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return True

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = None
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=None,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(None, token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=None)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", )
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("XXBearerXX", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("BEARER", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = None
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = False
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation(None, "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", None)
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", )
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("XXconnectXX", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("CONNECT", "API Key configurada, HttpClient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "XXAPI Key configurada, HttpClient inicializadoXX")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "api key configurada, httpclient inicializado")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API KEY CONFIGURADA, HTTPCLIENT INICIALIZADO")
        return True

    def xǁOpenaiV2Connectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de OpenAI."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("OpenaiV2Connector: API Key no configurada")
            return False

        api_key = self._get_api_key()
        if not api_key:
            logger.error("OpenaiV2Connector: No se pudo extraer la API Key del auth provider")
            return False

        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=api_key)
        self._connected = True
        self._log_operation("connect", "API Key configurada, HttpClient inicializado")
        return False

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXchat_completionXX": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "CHAT_COMPLETION": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "XXcreate_imageXX": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "CREATE_IMAGE": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "XXcreate_embeddingXX": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "CREATE_EMBEDDING": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "XXtranscribe_audioXX": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "TRANSCRIBE_AUDIO": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "XXgenerate_speechXX": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "GENERATE_SPEECH": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "XXlist_modelsXX": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "LIST_MODELS": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁOpenaiV2Connectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector OpenAI v2.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "chat_completion": self._chat_completion,
            "create_image": self._create_image,
            "create_embedding": self._create_embedding,
            "transcribe_audio": self._transcribe_audio,
            "generate_speech": self._generate_speech,
            "list_models": self._list_models,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que la API Key de OpenAI este configurada."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁOpenaiV2Connectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que la API Key de OpenAI este configurada."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁOpenaiV2Connectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que la API Key de OpenAI este configurada."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁOpenaiV2Connectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que la API Key de OpenAI este configurada."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁOpenaiV2Connectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con OpenAI."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ_chat_completion__mutmut)
    def _chat_completion(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = None
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get(None, [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", None)
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get([])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", )
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("XXmessagesXX", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("MESSAGES", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = None
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get(None, "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", None)
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", )
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("XXmodelXX", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("MODEL", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "XXgpt-4XX")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "GPT-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"XXsuccessXX": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"SUCCESS": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": True, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "XXerrorXX": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "ERROR": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "XXParametro requerido: messagesXX"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "PARAMETRO REQUERIDO: MESSAGES"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation(None, f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", None)

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation(f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", )

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("XXchat_completionXX", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("CHAT_COMPLETION", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = None
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "XXmodelXX": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "MODEL": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "XXmessagesXX": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "MESSAGES": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "XXtemperatureXX" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "TEMPERATURE" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" not in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = None
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["XXtemperatureXX"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["TEMPERATURE"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["XXtemperatureXX"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["TEMPERATURE"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "XXmax_tokensXX" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "MAX_TOKENS" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" not in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = None
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["XXmax_tokensXX"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["MAX_TOKENS"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["XXmax_tokensXX"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["MAX_TOKENS"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "XXtop_pXX" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "TOP_P" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" not in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = None
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["XXtop_pXX"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["TOP_P"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["XXtop_pXX"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["TOP_P"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "XXstreamXX" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "STREAM" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" not in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = None  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["XXstreamXX"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["STREAM"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = True  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post(None, json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=None, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post(json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("XX/chat/completionsXX", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/CHAT/COMPLETIONS", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=121)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁOpenaiV2Connectorǁ_chat_completion__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una respuesta de chat usando GPT.

        Args:
            params: Debe contener 'messages' (lista de dicts con role y content), opcionalmente 'model', 'temperature', 'max_tokens'
        """
        messages = params.get("messages", [])
        model = params.get("model", "gpt-4")
        if not messages:
            return {"success": False, "error": "Parametro requerido: messages"}

        self._log_operation("chat_completion", f"model={model}, messages={len(messages)}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "messages": messages,
            }
            if "temperature" in params:
                body["temperature"] = params["temperature"]
            if "max_tokens" in params:
                body["max_tokens"] = params["max_tokens"]
            if "top_p" in params:
                body["top_p"] = params["top_p"]
            if "stream" in params:
                body["stream"] = False  # Force non-streaming for simplicity

            response = self._http.post("/chat/completions", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.chat_completion: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.chat_completion: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ_create_image__mutmut)
    def _create_image(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = None
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get(None, "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", None)
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", )
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("XXpromptXX", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("PROMPT", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "XXXX")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = None
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get(None, "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", None)
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", )
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("XXmodelXX", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("MODEL", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "XXdall-e-3XX")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "DALL-E-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = None
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get(None, 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", None)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get(1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", )
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("XXnXX", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("N", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 2)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = None
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get(None, "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", None)
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", )
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("XXsizeXX", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("SIZE", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "XX1024x1024XX")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024X1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"XXsuccessXX": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"SUCCESS": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": True, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "XXerrorXX": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "ERROR": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "XXParametro requerido: promptXX"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PROMPT"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation(None, f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", None)

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation(f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", )

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("XXcreate_imageXX", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("CREATE_IMAGE", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = None
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "XXmodelXX": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "MODEL": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "XXpromptXX": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "PROMPT": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "XXnXX": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "N": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "XXsizeXX": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "SIZE": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "XXqualityXX" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "QUALITY" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" not in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = None
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["XXqualityXX"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["QUALITY"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["XXqualityXX"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["QUALITY"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "XXresponse_formatXX" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "RESPONSE_FORMAT" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" not in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = None

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["XXresponse_formatXX"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["RESPONSE_FORMAT"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["XXresponse_formatXX"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["RESPONSE_FORMAT"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post(None, json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=None, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post(json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("XX/images/generationsXX", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/IMAGES/GENERATIONS", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=121)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁOpenaiV2Connectorǁ_create_image__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una imagen usando DALL-E.

        Args:
            params: Debe contener 'prompt' y opcionalmente 'model', 'n', 'size', 'quality'
        """
        prompt = params.get("prompt", "")
        model = params.get("model", "dall-e-3")
        n = params.get("n", 1)
        size = params.get("size", "1024x1024")
        if not prompt:
            return {"success": False, "error": "Parametro requerido: prompt"}

        self._log_operation("create_image", f"model={model}, size={size}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "prompt": prompt,
                "n": n,
                "size": size,
            }
            if "quality" in params:
                body["quality"] = params["quality"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            response = self._http.post("/images/generations", json=body, timeout=120)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_image: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_image: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ_create_embedding__mutmut)
    def _create_embedding(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = None
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get(None, "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", None)
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", )
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("XXinputXX", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("INPUT", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "XXXX")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = None
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get(None, "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", None)
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", )
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("XXmodelXX", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("MODEL", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "XXtext-embedding-3-smallXX")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "TEXT-EMBEDDING-3-SMALL")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"XXsuccessXX": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"SUCCESS": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": True, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "XXerrorXX": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "ERROR": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "XXParametro requerido: inputXX"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "PARAMETRO REQUERIDO: INPUT"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation(None, f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", None)

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation(f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", )

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("XXcreate_embeddingXX", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("CREATE_EMBEDDING", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = None
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "XXmodelXX": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "MODEL": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "XXinputXX": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "INPUT": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "XXencoding_formatXX" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "ENCODING_FORMAT" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" not in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = None
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["XXencoding_formatXX"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["ENCODING_FORMAT"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["XXencoding_formatXX"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["ENCODING_FORMAT"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "XXdimensionsXX" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "DIMENSIONS" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" not in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = None

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["XXdimensionsXX"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["DIMENSIONS"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["XXdimensionsXX"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["DIMENSIONS"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post(None, json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=None, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post(json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("XX/embeddingsXX", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/EMBEDDINGS", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=61)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁOpenaiV2Connectorǁ_create_embedding__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera embeddings usando text-embedding.

        Args:
            params: Debe contener 'input' y opcionalmente 'model'
        """
        input_text = params.get("input", "")
        model = params.get("model", "text-embedding-3-small")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("create_embedding", f"model={model}")

        try:
            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
            }
            if "encoding_format" in params:
                body["encoding_format"] = params["encoding_format"]
            if "dimensions" in params:
                body["dimensions"] = params["dimensions"]

            response = self._http.post("/embeddings", json=body, timeout=60)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.create_embedding: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.create_embedding: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut)
    def _transcribe_audio(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = None
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get(None, "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", None)
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", )
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("XXfileXX", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("FILE", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "XXXX")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = None
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get(None, "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", None)
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", )
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("XXmodelXX", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("MODEL", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "XXwhisper-1XX")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "WHISPER-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"XXsuccessXX": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"SUCCESS": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": True, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "XXerrorXX": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "ERROR": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "XXParametro requerido: fileXX"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "PARAMETRO REQUERIDO: FILE"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation(None, f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", None)

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation(f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", )

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("XXtranscribe_audioXX", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("TRANSCRIBE_AUDIO", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = None
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = None

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = None
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"XXmodelXX": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"MODEL": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "XXlanguageXX" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "LANGUAGE" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" not in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = None

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["XXlanguageXX"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["LANGUAGE"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["XXlanguageXX"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["LANGUAGE"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = ""
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(None):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(None, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, None) as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open("rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, ) as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "XXrbXX") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "RB") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = None
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = None
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"XXfileXX": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"FILE": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(None), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = None
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(None)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = None
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"XXfileXX": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"FILE": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("XXaudio.wavXX", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("AUDIO.WAV", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"XXsuccessXX": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"SUCCESS": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": True, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "XXerrorXX": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "ERROR": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "XXEl parametro 'file' debe ser una ruta de archivo valida o base64XX"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "el parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "EL PARAMETRO 'FILE' DEBE SER UNA RUTA DE ARCHIVO VALIDA O BASE64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = None

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                None,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers=None,
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=None,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=None,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=None,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"XXAuthorizationXX": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"AUTHORIZATION": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=121,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 201 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 < resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code <= 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 301:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"XXsuccessXX": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"SUCCESS": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": True, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "XXerrorXX": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "ERROR": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = None
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"XXsuccessXX": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"SUCCESS": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": False, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Transcribe audio usando Whisper.

        Uses the requests library directly since Whisper API requires
        multipart/form-data which HttpClient doesn't support natively.

        Args:
            params: Debe contener 'file' (ruta o base64) y opcionalmente 'model', 'language'
        """
        file_path = params.get("file", "")
        model = params.get("model", "whisper-1")
        if not file_path:
            return {"success": False, "error": "Parametro requerido: file"}

        self._log_operation("transcribe_audio", f"model={model}")

        try:
            import base64
            import os

            import requests as req_lib

            api_key = self._get_api_key()
            url = f"{self._base_url}/audio/transcriptions"

            # Handle file: could be a path or base64-encoded data
            form_data = {"model": model}
            if "language" in params:
                form_data["language"] = params["language"]

            files_payload = None
            if os.path.isfile(file_path):
                with open(file_path, "rb") as f:
                    audio_bytes = f.read()
                files_payload = {"file": (os.path.basename(file_path), audio_bytes)}
            else:
                # Assume base64-encoded audio
                try:
                    audio_bytes = base64.b64decode(file_path)
                    files_payload = {"file": ("audio.wav", audio_bytes)}
                except Exception:
                    return {"success": False, "error": "El parametro 'file' debe ser una ruta de archivo valida o base64"}

            resp = req_lib.post(
                url,
                headers={"Authorization": f"Bearer {api_key}"},
                files=files_payload,
                data=form_data,
                timeout=120,
            )

            if not 200 <= resp.status_code < 300:
                return {"success": False, "error": f"OpenAI API error ({resp.status_code}): {resp.text}"}

            data = resp.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.transcribe_audio: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut)
    def _generate_speech(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = None
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get(None, "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", None)
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", )
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("XXinputXX", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("INPUT", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "XXXX")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = None
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get(None, "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", None)
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", )
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("XXvoiceXX", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("VOICE", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "XXalloyXX")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "ALLOY")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = None
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get(None, "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", None)
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", )
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("XXmodelXX", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("MODEL", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "XXtts-1XX")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "TTS-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"XXsuccessXX": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"SUCCESS": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": True, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "XXerrorXX": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "ERROR": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "XXParametro requerido: inputXX"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "PARAMETRO REQUERIDO: INPUT"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation(None, f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", None)

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation(f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", )

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("XXgenerate_speechXX", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("GENERATE_SPEECH", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = None
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "XXmodelXX": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "MODEL": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "XXinputXX": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "INPUT": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "XXvoiceXX": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "VOICE": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "XXspeedXX" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "SPEED" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" not in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = None
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["XXspeedXX"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["SPEED"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["XXspeedXX"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["SPEED"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "XXresponse_formatXX" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "RESPONSE_FORMAT" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" not in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = None

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["XXresponse_formatXX"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["RESPONSE_FORMAT"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["XXresponse_formatXX"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["RESPONSE_FORMAT"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                None,
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=None,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=None,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers=None,
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "XX/audio/speechXX",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/AUDIO/SPEECH",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=121,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"XXAcceptXX": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"ACCEPT": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "XXaudio/mpegXX"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "AUDIO/MPEG"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = None
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = "XXXX"
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = None

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode(None)

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(None).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("XXutf-8XX")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("UTF-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "XXsuccessXX": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "SUCCESS": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": False,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "XXaudio_base64XX": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "AUDIO_BASE64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "XXcontent_typeXX": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "CONTENT_TYPE": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "XXaudio/mpegXX",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "AUDIO/MPEG",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "XXmodelXX": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "MODEL": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "XXvoiceXX": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "VOICE": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁOpenaiV2Connectorǁ_generate_speech__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera audio a partir de texto usando TTS.

        Args:
            params: Debe contener 'input', 'voice' y opcionalmente 'model', 'speed'
        """
        input_text = params.get("input", "")
        voice = params.get("voice", "alloy")
        model = params.get("model", "tts-1")
        if not input_text:
            return {"success": False, "error": "Parametro requerido: input"}

        self._log_operation("generate_speech", f"voice={voice}")

        try:
            import base64

            body: dict[str, Any] = {
                "model": model,
                "input": input_text,
                "voice": voice,
            }
            if "speed" in params:
                body["speed"] = params["speed"]
            if "response_format" in params:
                body["response_format"] = params["response_format"]

            # TTS returns binary audio, so we use HttpClient but handle the raw response
            response = self._http.post(
                "/audio/speech",
                json=body,
                timeout=120,
                headers={"Accept": "audio/mpeg"},
            )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            # Response is binary audio data
            audio_base64 = ""
            if response.raw:
                audio_base64 = base64.b64encode(response.raw).decode("utf-8")

            return {
                "success": True,
                "audio_base64": audio_base64,
                "content_type": "audio/mpeg",
                "model": model,
                "voice": voice,
            }

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.generate_speech: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.generate_speech: error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut)
    def _list_models(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation(None)

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("XXlist_modelsXX")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("LIST_MODELS")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = None

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get(None, timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=None)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get(timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", )

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("XX/modelsXX", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/MODELS", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=31)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = None
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() and response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"XXsuccessXX": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"SUCCESS": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": True, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "XXerrorXX": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "ERROR": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = None
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"XXsuccessXX": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"SUCCESS": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": False, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"SUCCESS": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": True, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "XXerrorXX": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "ERROR": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(None)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": True, "error": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁOpenaiV2Connectorǁ_list_models__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los modelos disponibles en OpenAI."""
        self._log_operation("list_models")

        try:
            response = self._http.get("/models", timeout=30)

            if not response.ok:
                error_body = response.json() or response.body
                return {"success": False, "error": f"OpenAI API error ({response.status_code}): {error_body}"}

            data = response.json()
            return {"success": True, **data}

        except HTTPClientError as e:
            logger.error(f"OpenaiV2Connector.list_models: HTTP error: {e}")
            return {"success": False, "error": str(e)}
        except Exception as e:
            logger.error(f"OpenaiV2Connector.list_models: error: {e}")
            return {"success": False, "error": str(None)}

mutants_xǁOpenaiV2Connectorǁ__init____mutmut['_mutmut_orig'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ__init____mutmut['xǁOpenaiV2Connectorǁ__init____mutmut_1'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ__init____mutmut['xǁOpenaiV2Connectorǁ__init____mutmut_2'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ__init____mutmut['xǁOpenaiV2Connectorǁ__init____mutmut_3'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ__init____mutmut['xǁOpenaiV2Connectorǁ__init____mutmut_4'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['_mutmut_orig'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_1'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_2'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_3'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_4'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_5'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_6'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_7'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_8'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_9'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_10'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_11'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_12'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_13'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_14'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_15'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_16'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_17'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_18'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_19'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_20'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_21'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_22'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_23'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_24'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_25'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_26'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_27'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_28'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_29'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_30'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_31'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_32'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_33'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_34'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_35'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_36'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_37'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_37 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_38'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_38 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_39'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_39 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_40'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_40 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_41'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_41 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_42'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_42 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_43'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_43 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_44'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_44 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_45'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_45 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_46'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_46 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_47'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_47 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_48'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_48 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_get_api_key__mutmut['xǁOpenaiV2Connectorǁ_get_api_key__mutmut_49'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_get_api_key__mutmut_49 # type: ignore # mutmut generated

mutants_xǁOpenaiV2Connectorǁconnect__mutmut['_mutmut_orig'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_1'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_2'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_3'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_4'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_5'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_6'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_7'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_8'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_9'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_10'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_11'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_12'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_13'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_14'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_15'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_16'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_17'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_18'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_19'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_20'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_21'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_22'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_23'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_24'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_25'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_26'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_27'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_28'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_29'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_30'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_31'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_32'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_33'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_34'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_35'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut_36'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁconnect__mutmut['xǁOpenaiV2Connectorǁconnect__mutmut
] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_110 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut['xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_111'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_111 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut['xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_112'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_112 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut['xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_113'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_113 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut['xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_114'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_114 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut['xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_115'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_115 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut['xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_116'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_transcribe_audio__mutmut_116 # type: ignore # mutmut generated

mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['_mutmut_orig'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_1'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_2'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_3'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_4'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_5'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_6'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_7'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_8'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_9'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_10'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_11'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_12'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_13'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_14'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_15'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_16'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_17'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_18'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_19'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_20'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_21'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_22'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_23'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_24'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_25'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_26'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_27'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_28'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_29'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_30'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_31'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_32'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_33'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_34'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_35'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_36'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_37'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_37 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_38'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_38 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_39'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_39 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_40'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_40 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_41'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_41 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_42'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_42 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_43'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_43 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_44'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_44 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_45'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_45 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_46'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_46 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_47'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_47 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_48'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_48 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_49'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_49 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_50'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_50 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_51'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_51 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_52'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_52 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_53'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_53 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_54'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_54 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_55'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_55 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_56'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_56 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_57'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_57 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_58'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_58 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_59'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_59 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_60'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_60 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_61'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_61 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_62'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_62 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_63'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_63 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_64'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_64 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_65'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_65 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_66'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_66 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_67'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_67 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_68'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_68 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_69'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_69 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_70'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_70 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_71'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_71 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_72'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_72 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_73'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_73 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_74'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_74 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_75'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_75 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_76'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_76 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_77'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_77 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_78'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_78 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_79'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_79 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_80'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_80 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_81'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_81 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_82'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_82 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_83'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_83 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_84'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_84 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_85'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_85 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_86'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_86 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_87'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_87 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_88'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_88 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_89'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_89 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_90'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_90 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_91'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_91 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_92'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_92 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_93'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_93 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_94'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_94 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_95'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_95 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_96'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_96 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_97'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_97 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_98'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_98 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_99'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_99 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_100'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_100 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_101'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_101 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_102'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_102 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_103'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_103 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_104'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_104 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_105'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_105 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_106'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_106 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_107'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_107 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_108'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_108 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_109'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_109 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_110'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_110 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_111'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_111 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_112'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_112 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_113'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_113 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_114'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_114 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_115'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_115 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_116'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_116 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_117'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_117 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_118'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_118 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_119'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_119 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_120'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_120 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_121'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_121 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_122'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_122 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_generate_speech__mutmut['xǁOpenaiV2Connectorǁ_generate_speech__mutmut_123'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_generate_speech__mutmut_123 # type: ignore # mutmut generated

mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['_mutmut_orig'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_1'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_2'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_3'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_4'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_5'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_6'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_7'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_8'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_9'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_10'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_11'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_12'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_13'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_14'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_15'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_16'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_17'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_18'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_19'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_20'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_21'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_22'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_23'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_24'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_25'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_26'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_27'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_28'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_29'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_30'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_31'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_32'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_33'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_34'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_35'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_36'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOpenaiV2Connectorǁ_list_models__mutmut['xǁOpenaiV2Connectorǁ_list_models__mutmut_37'] = OpenaiV2Connector.xǁOpenaiV2Connectorǁ_list_models__mutmut_37 # type: ignore # mutmut generated


OPENAI_V2_SCHEMA = ConnectorSchema(
    name="openai_v2",
    version="2.0.0",
    description="Genera texto con GPT, imagenes con DALL-E, embeddings y audio via OpenAI",
    category="ai_data",
    icon="sparkles",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="chat_completion", description="Genera respuesta de chat", category="write"),
        ActionDefinition(name="create_image", description="Genera imagen con DALL-E", category="write"),
        ActionDefinition(name="create_embedding", description="Genera embeddings", category="write"),
        ActionDefinition(name="transcribe_audio", description="Transcribe audio con Whisper", category="read"),
        ActionDefinition(name="generate_speech", description="Genera audio con TTS", category="write"),
        ActionDefinition(name="list_models", description="Lista modelos disponibles", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="api_key", required_fields=["api_key"], description="OpenAI API Key")
    ],
)
