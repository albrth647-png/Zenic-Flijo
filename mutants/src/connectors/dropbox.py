"""
Conector Dropbox — Operaciones de Almacenamiento de Archivos
================================================================

Permite subir, descargar, listar y gestionar archivos en
cuentas de Dropbox usando la API v2 (RPC-style).
"""

from __future__ import annotations

import json
import os
from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁDropboxConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁ_upload_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁ_download_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁ_list_folder__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁ_delete_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁ_create_folder__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut: MutantDict = {}  # type: ignore


class DropboxConnector(BaseConnector):
    """Conector para Dropbox: operaciones CRUD en archivos y carpetas.

    Usa la Dropbox API v2 (https://api.dropboxapi.com/2/) con
    autenticacion OAuth2 Bearer token via HttpClient.
    """

    name = "dropbox"
    version = "1.0.0"
    description = "Sube, descarga y gestiona archivos en cuentas de Dropbox"
    category = "cloud_storage"
    icon = "folder"
    author = "Zenic-Flijo"

    # Dropbox API endpoints
    _API_BASE = "https://api.dropboxapi.com/2"
    _CONTENT_BASE = "https://content.dropboxapi.com/2"

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._API_BASE
        self._http: HttpClient | None = None
        self._content_http: HttpClient | None = None
        self._access_token: str = ""

    def xǁDropboxConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._API_BASE
        self._http: HttpClient | None = None
        self._content_http: HttpClient | None = None
        self._access_token: str = ""

    def xǁDropboxConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None
        self._content_http: HttpClient | None = None
        self._access_token: str = ""

    def xǁDropboxConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._API_BASE
        self._http: HttpClient | None = ""
        self._content_http: HttpClient | None = None
        self._access_token: str = ""

    def xǁDropboxConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._API_BASE
        self._http: HttpClient | None = None
        self._content_http: HttpClient | None = ""
        self._access_token: str = ""

    def xǁDropboxConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._API_BASE
        self._http: HttpClient | None = None
        self._content_http: HttpClient | None = None
        self._access_token: str = None

    def xǁDropboxConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = self._API_BASE
        self._http: HttpClient | None = None
        self._content_http: HttpClient | None = None
        self._access_token: str = "XXXX"

    # ── Conexion ────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXDropboxConnector: credenciales OAuth2 no configuradasXX")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("dropboxconnector: credenciales oauth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DROPBOXCONNECTOR: CREDENCIALES OAUTH2 NO CONFIGURADAS")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return True

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = None
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(None, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, None) else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr("get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, ) else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "XXget_credentialsXX") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "GET_CREDENTIALS") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = None

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get(None, os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", None)

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get(os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", )

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("XXaccess_tokenXX", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("ACCESS_TOKEN", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get(None, ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", None))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get(""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("XXDROPBOX_ACCESS_TOKENXX", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("dropbox_access_token", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", "XXXX"))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error(None)
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("XXDropboxConnector: access_token no proporcionadoXX")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("dropboxconnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DROPBOXCONNECTOR: ACCESS_TOKEN NO PROPORCIONADO")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return True

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = None
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=None,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=None,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers=None,
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=31,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "XXContent-TypeXX": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "content-type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "CONTENT-TYPE": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "XXapplication/jsonXX",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "APPLICATION/JSON",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth(None, token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=None)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth(token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", )

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("XXBearerXX", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("BEARER", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = None
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=None,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=None,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=None,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers=None,
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=61,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "XXContent-TypeXX": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "content-type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "CONTENT-TYPE": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "XXapplication/octet-streamXX",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "APPLICATION/OCTET-STREAM",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth(None, token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=None)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth(token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", )

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("XXBearerXX", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("BEARER", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = None
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post(None)
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("XX/users/get_current_accountXX")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/USERS/GET_CURRENT_ACCOUNT")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = None
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = None
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get(None, "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", None) if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", ) if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("XXaccount_idXX", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("ACCOUNT_ID", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "XXXX") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else "XXXX"
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = None
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = False
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation(None, f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", None)
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation(f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", )
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("XXconnectXX", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("CONNECT", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return False
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(None)
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return True
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(None)
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁDropboxConnectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con la API de Dropbox."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DropboxConnector: credenciales OAuth2 no configuradas")
            return False

        # Extraer credenciales
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_token = creds.get("access_token", os.environ.get("DROPBOX_ACCESS_TOKEN", ""))

        if not self._access_token:
            logger.error("DropboxConnector: access_token no proporcionado")
            return False

        # Inicializar HttpClients
        self._http = HttpClient(
            base_url=self._API_BASE,
            connector_name=self.name,
            timeout=30,
            default_headers={
                "Content-Type": "application/json",
            },
        )
        self._http.set_auth("Bearer", token=self._access_token)

        self._content_http = HttpClient(
            base_url=self._CONTENT_BASE,
            connector_name=self.name,
            timeout=60,
            default_headers={
                "Content-Type": "application/octet-stream",
            },
        )
        self._content_http.set_auth("Bearer", token=self._access_token)

        # Verificar conexion obteniendo la cuenta del usuario
        try:
            resp = self._http.post("/users/get_current_account")
            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                account_id = body.get("account_id", "") if isinstance(body, dict) else ""
                self._connected = True
                self._log_operation("connect", f"Dropbox API OK, account_id={account_id}")
                return True
            logger.error(f"DropboxConnector: API respondio {resp.status_code}: {resp.body}")
            return False
        except HTTPClientError as exc:
            logger.error(f"DropboxConnector: API fallo: {exc}")
            return True

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "XXupload_fileXX": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "UPLOAD_FILE": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "XXdownload_fileXX": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "DOWNLOAD_FILE": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "XXlist_folderXX": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "LIST_FOLDER": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "XXdelete_fileXX": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "DELETE_FILE": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "XXcreate_folderXX": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "CREATE_FOLDER": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "XXget_temporary_linkXX": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "GET_TEMPORARY_LINK": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁDropboxConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Dropbox."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_folder": self._list_folder,
            "delete_file": self._delete_file,
            "create_folder": self._create_folder,
            "get_temporary_link": self._get_temporary_link,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales de Dropbox esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁDropboxConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales de Dropbox esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁDropboxConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales de Dropbox esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁDropboxConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales de Dropbox esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = None
        self._content_http = None
        self._log_operation("disconnect")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = None
        self._content_http = None
        self._log_operation("disconnect")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = None
        self._http = None
        self._content_http = None
        self._log_operation("disconnect")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = True
        self._http = None
        self._content_http = None
        self._log_operation("disconnect")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = ""
        self._content_http = None
        self._log_operation("disconnect")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = None
        self._content_http = ""
        self._log_operation("disconnect")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = None
        self._content_http = None
        self._log_operation(None)
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = None
        self._content_http = None
        self._log_operation("XXdisconnectXX")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = None
        self._content_http = None
        self._log_operation("DISCONNECT")
        return True

    def xǁDropboxConnectorǁdisconnect__mutmut_8(self) -> bool:
        """Cierra la conexion con Dropbox."""
        self._connected = False
        self._http = None
        self._content_http = None
        self._log_operation("disconnect")
        return False

    # ── Acciones ───────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁ_upload_file__mutmut)
    def _upload_file(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = None
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get(None, "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", None)
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", )
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("XXpathXX", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("PATH", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "XXXX")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = None
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get(None)
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("XXcontentXX")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("CONTENT")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = None
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get(None)
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("XXfile_pathXX")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("FILE_PATH")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = None  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get(None, "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", None)  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", )  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("XXmodeXX", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("MODE", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "XXaddXX")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "ADD")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = None
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get(None, False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", None)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get(False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", )
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("XXautorenameXX", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("AUTORENAME", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", True)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = None

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get(None, False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", None)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get(False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", )

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("XXmuteXX", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("MUTE", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", True)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"XXsuccessXX": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"SUCCESS": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": True, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "XXerrorXX": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "ERROR": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "XXParametro requerido: pathXX"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PATH"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None or not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is not None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"XXsuccessXX": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"SUCCESS": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": True, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "XXerrorXX": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "ERROR": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "XXDebe proporcionar 'content' o 'file_path'XX"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "DEBE PROPORCIONAR 'CONTENT' O 'FILE_PATH'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None or file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is not None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(None, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, None) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open("rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, ) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "XXrbXX") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "RB") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = None
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"XXsuccessXX": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"SUCCESS": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": True, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "XXerrorXX": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "ERROR": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = None

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode(None)

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("XXutf-8XX")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("UTF-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation(None, f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", None)

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation(f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", )

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("XXupload_fileXX", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("UPLOAD_FILE", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = None

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps(None)

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "XXpathXX": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "PATH": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "XXmodeXX": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "MODE": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "XXautorenameXX": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "AUTORENAME": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "XXmuteXX": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "MUTE": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = None
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = None

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                None,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=None,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers=None,
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=None,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "XXAuthorizationXX": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "AUTHORIZATION": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "XXContent-TypeXX": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "content-type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "CONTENT-TYPE": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "XXapplication/octet-streamXX",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "APPLICATION/OCTET-STREAM",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "XXDropbox-API-ArgXX": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "dropbox-api-arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "DROPBOX-API-ARG": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=61,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 201 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 < resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code <= 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 301:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = None
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "XXsuccessXX": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "SUCCESS": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": False,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "XXpathXX": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "PATH": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get(None, path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", None),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get(path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", ),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("XXpath_displayXX", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("PATH_DISPLAY", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "XXidXX": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "ID": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get(None, ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", None),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get(""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("XXidXX", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("ID", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", "XXXX"),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "XXsizeXX": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "SIZE": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get(None, len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", None),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get(len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", ),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("XXsizeXX", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("SIZE", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "XXrevisionXX": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "REVISION": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get(None, ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", None),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get(""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("XXrevXX", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("REV", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", "XXXX"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"XXsuccessXX": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"SUCCESS": False, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": True, "error": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "XXerrorXX": f"Error inesperado: {exc}"}

    # ── Acciones ───────────────────────────────────────────────

    def xǁDropboxConnectorǁ_upload_file__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a Dropbox usando upload session.

        Args:
            params: Debe contener 'path' y 'content' (o 'file_path')
        """
        path = params.get("path", "")
        content = params.get("content")
        file_path = params.get("file_path")
        mode = params.get("mode", "add")  # add, overwrite, update
        autorename = params.get("autorename", False)
        mute = params.get("mute", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        self._log_operation("upload_file", f"path={path}, size={len(content)}")

        if not self._content_http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Usar content-upload endpoint de Dropbox
            dropbox_api_args = json.dumps({
                "path": path,
                "mode": mode,
                "autorename": autorename,
                "mute": mute,
            })

            # Dropbox upload usa el endpoint de contenido
            import requests as req_lib

            url = f"{self._CONTENT_BASE}/files/upload"
            resp = req_lib.post(
                url,
                data=content,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Content-Type": "application/octet-stream",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                result = resp.json()
                return {
                    "success": True,
                    "path": result.get("path_display", path),
                    "id": result.get("id", ""),
                    "size": result.get("size", len(content)),
                    "revision": result.get("rev", ""),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "ERROR": f"Error inesperado: {exc}"}

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁ_download_file__mutmut)
    def _download_file(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = None
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get(None, "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", None)
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", )
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("XXpathXX", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("PATH", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "XXXX")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"XXsuccessXX": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"SUCCESS": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": True, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "XXerrorXX": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "ERROR": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "XXParametro requerido: pathXX"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PATH"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(None, f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", None)

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", )

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("XXdownload_fileXX", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("DOWNLOAD_FILE", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = None

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps(None)

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"XXpathXX": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"PATH": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = None
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = None

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                None,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers=None,
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=None,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "XXAuthorizationXX": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "AUTHORIZATION": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "XXDropbox-API-ArgXX": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "dropbox-api-arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "DROPBOX-API-ARG": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=61,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 201 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 < resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code <= 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 301:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = None
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get(None, "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", None)
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", )
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("XXDropbox-API-ResultXX", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("dropbox-api-result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("DROPBOX-API-RESULT", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "XX{}XX")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = None
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(None)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = None

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "XXsuccessXX": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "SUCCESS": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": False,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "XXpathXX": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "PATH": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "XXcontentXX": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "CONTENT": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "XXsize_bytesXX": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "SIZE_BYTES": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "XXmetadataXX": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "METADATA": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"XXsuccessXX": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"SUCCESS": False, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": True, "error": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "XXerrorXX": f"Error inesperado: {exc}"}

    def xǁDropboxConnectorǁ_download_file__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("download_file", f"path={path}")

        try:
            import requests as req_lib

            dropbox_api_args = json.dumps({"path": path})

            url = f"{self._CONTENT_BASE}/files/download"
            resp = req_lib.post(
                url,
                headers={
                    "Authorization": f"Bearer {self._access_token}",
                    "Dropbox-API-Arg": dropbox_api_args,
                },
                timeout=60,
            )

            if 200 <= resp.status_code < 300:
                # Dropbox devuelve metadata en el header Dropbox-API-Result
                api_result = resp.headers.get("Dropbox-API-Result", "{}")
                try:
                    metadata = json.loads(api_result)
                except json.JSONDecodeError:
                    metadata = {}

                return {
                    "success": True,
                    "path": path,
                    "content": resp.content,
                    "size_bytes": len(resp.content),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.text}"}

        except Exception as exc:
            return {"success": False, "ERROR": f"Error inesperado: {exc}"}

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁ_list_folder__mutmut)
    def _list_folder(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = None
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get(None, "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", None)
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", )
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("XXpathXX", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("PATH", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "XXXX")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = None
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get(None, 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", None)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get(100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", )
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("XXlimitXX", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("LIMIT", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 101)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = None
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get(None, False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", None)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get(False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", )
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("XXrecursiveXX", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("RECURSIVE", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", True)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = None

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get(None, "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", None)

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", )

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("XXcursorXX", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("CURSOR", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "XXXX")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation(None, f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation(f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("XXlist_folderXX", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("LIST_FOLDER", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = None
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    None,
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json=None,
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "XX/files/list_folder/continueXX",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/FILES/LIST_FOLDER/CONTINUE",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"XXcursorXX": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"CURSOR": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = None

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    None,
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json=None,
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "XX/files/list_folderXX",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/FILES/LIST_FOLDER",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "XXpathXX": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "PATH": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "XXrecursiveXX": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "RECURSIVE": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "XXlimitXX": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "LIMIT": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "XXinclude_media_infoXX": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "INCLUDE_MEDIA_INFO": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": True,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "XXinclude_deletedXX": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "INCLUDE_DELETED": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": True,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "XXinclude_has_explicit_shared_membersXX": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "INCLUDE_HAS_EXPLICIT_SHARED_MEMBERS": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": True,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = None
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"XXsuccessXX": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"SUCCESS": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": True, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "XXerrorXX": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "ERROR": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "XXRespuesta inesperada de Dropbox APIXX"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "respuesta inesperada de dropbox api"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "RESPUESTA INESPERADA DE DROPBOX API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = None
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get(None, []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", None):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get([]):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", ):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("XXentriesXX", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("ENTRIES", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = None
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "XXnameXX": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "NAME": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get(None, ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", None),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get(""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("XXnameXX", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("NAME", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", "XXXX"),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "XXpath_lowerXX": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "PATH_LOWER": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get(None, ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", None),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get(""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("XXpath_lowerXX", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("PATH_LOWER", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", "XXXX"),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "XXpath_displayXX": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "PATH_DISPLAY": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get(None, ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", None),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get(""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("XXpath_displayXX", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("PATH_DISPLAY", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", "XXXX"),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "XXtypeXX": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "TYPE": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(None, ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", None),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get("XX.tagXX", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".TAG", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", "XXXX"),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(None) == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get("XX.tagXX") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".TAG") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") != "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "XXfileXX":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "FILE":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = None
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["XXsizeXX"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["SIZE"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get(None, 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", None)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get(0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", )
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("XXsizeXX", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("SIZE", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 1)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = None
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["XXcontent_typeXX"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["CONTENT_TYPE"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get(None, "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", None)
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", )
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("XXclient_modifiedXX", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("CLIENT_MODIFIED", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "XXXX")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = None
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["XXrevisionXX"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["REVISION"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get(None, "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", None)
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", )
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("XXrevXX", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("REV", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "XXXX")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = None
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["XXclient_modifiedXX"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["CLIENT_MODIFIED"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get(None, "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", None)
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", )
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("XXclient_modifiedXX", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("CLIENT_MODIFIED", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "XXXX")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = None
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["XXserver_modifiedXX"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["SERVER_MODIFIED"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get(None, "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", None)
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", )
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("XXserver_modifiedXX", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("SERVER_MODIFIED", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "XXXX")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(None)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "XXsuccessXX": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "SUCCESS": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": False,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "XXpathXX": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "PATH": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "XXentriesXX": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "ENTRIES": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "XXtotalXX": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "TOTAL": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "XXhas_moreXX": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "HAS_MORE": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get(None, False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", None),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_209(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get(False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_210(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", ),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_211(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("XXhas_moreXX", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_212(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("HAS_MORE", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_213(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", True),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_214(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "XXcursorXX": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_215(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "CURSOR": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_216(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get(None, ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_217(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", None),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_218(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get(""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_219(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_220(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("XXcursorXX", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_221(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("CURSOR", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_222(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", "XXXX"),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_223(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_224(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_225(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_226(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_227(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁDropboxConnectorǁ_list_folder__mutmut_228(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista el contenido de una carpeta de Dropbox con paginacion.

        Args:
            params: Debe contener 'path' y opcionalmente 'limit', 'recursive', 'cursor'
        """
        path = params.get("path", "")
        limit = params.get("limit", 100)
        recursive = params.get("recursive", False)
        cursor = params.get("cursor", "")

        self._log_operation("list_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            # Si hay cursor, continuar listando
            if cursor:
                resp = self._http.post(
                    "/files/list_folder/continue",
                    json={"cursor": cursor},
                )
            else:
                resp = self._http.post(
                    "/files/list_folder",
                    json={
                        "path": path,
                        "recursive": recursive,
                        "limit": limit,
                        "include_media_info": False,
                        "include_deleted": False,
                        "include_has_explicit_shared_members": False,
                    },
                )

            if not resp.ok:
                return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}

            body = resp.json() if isinstance(resp.body, str) else resp.body
            if not isinstance(body, dict):
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}

            entries = []
            for entry in body.get("entries", []):
                entry_info: dict[str, Any] = {
                    "name": entry.get("name", ""),
                    "path_lower": entry.get("path_lower", ""),
                    "path_display": entry.get("path_display", ""),
                    "type": entry.get(".tag", ""),  # file or folder
                }
                # Metadata especifica de archivos
                if entry.get(".tag") == "file":
                    entry_info["size"] = entry.get("size", 0)
                    entry_info["content_type"] = entry.get("client_modified", "")
                    entry_info["revision"] = entry.get("rev", "")
                    entry_info["client_modified"] = entry.get("client_modified", "")
                    entry_info["server_modified"] = entry.get("server_modified", "")
                entries.append(entry_info)

            return {
                "success": True,
                "path": path,
                "entries": entries,
                "total": len(entries),
                "has_more": body.get("has_more", False),
                "cursor": body.get("cursor", ""),
            }

        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁ_delete_file__mutmut)
    def _delete_file(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = None
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get(None, "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", None)
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", )
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("XXpathXX", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("PATH", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "XXXX")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"XXsuccessXX": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"SUCCESS": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": True, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "XXerrorXX": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "ERROR": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "XXParametro requerido: pathXX"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PATH"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(None, f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("XXdelete_fileXX", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("DELETE_FILE", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = None

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                None,
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json=None,
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "XX/files/delete_v2XX",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/FILES/DELETE_V2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"XXpathXX": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"PATH": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = None
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = None
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get(None, {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", None) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get({}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", ) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("XXmetadataXX", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("METADATA", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "XXsuccessXX": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "SUCCESS": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": False,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "XXpathXX": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "PATH": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get(None, path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", None),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get(path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", ),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("XXpath_displayXX", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("PATH_DISPLAY", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "XXdeletedXX": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "DELETED": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": False,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "XXmetadataXX": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "METADATA": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁDropboxConnectorǁ_delete_file__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un archivo o carpeta de Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("delete_file", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/delete_v2",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "deleted": True,
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁ_create_folder__mutmut)
    def _create_folder(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = None
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get(None, "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", None)
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", )
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("XXpathXX", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("PATH", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "XXXX")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = None

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get(None, False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", None)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get(False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", )

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("XXautorenameXX", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("AUTORENAME", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", True)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"XXsuccessXX": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"SUCCESS": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": True, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "XXerrorXX": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "ERROR": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "XXParametro requerido: pathXX"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PATH"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(None, f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("XXcreate_folderXX", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("CREATE_FOLDER", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = None

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                None,
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json=None,
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "XX/files/create_folder_v2XX",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/FILES/CREATE_FOLDER_V2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"XXpathXX": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"PATH": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "XXautorenameXX": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "AUTORENAME": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = None
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = None
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get(None, {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", None) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get({}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", ) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("XXmetadataXX", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("METADATA", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "XXsuccessXX": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "SUCCESS": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": False,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "XXpathXX": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "PATH": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get(None, path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", None),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get(path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", ),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("XXpath_displayXX", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("PATH_DISPLAY", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "XXidXX": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "ID": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get(None, ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", None),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get(""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("XXidXX", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("ID", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", "XXXX"),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "XXmetadataXX": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "METADATA": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁDropboxConnectorǁ_create_folder__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una carpeta en Dropbox.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        autorename = params.get("autorename", False)

        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("create_folder", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/create_folder_v2",
                json={"path": path, "autorename": autorename},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                metadata = body.get("metadata", {}) if isinstance(body, dict) else {}
                return {
                    "success": True,
                    "path": metadata.get("path_display", path),
                    "id": metadata.get("id", ""),
                    "metadata": metadata,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut)
    def _get_temporary_link(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = None
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get(None, "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", None)
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", )
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("XXpathXX", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("PATH", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "XXXX")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"XXsuccessXX": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"SUCCESS": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": True, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "XXerrorXX": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "ERROR": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "XXParametro requerido: pathXX"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PATH"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(None, f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", None)

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation(f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", )

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("XXget_temporary_linkXX", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("GET_TEMPORARY_LINK", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"SUCCESS": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": True, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "XXerrorXX": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "ERROR": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "XXHttpClient no inicializado. Llame connect() primero.XX"}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "httpclient no inicializado. llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = None

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                None,
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json=None,
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "XX/files/get_temporary_linkXX",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/FILES/GET_TEMPORARY_LINK",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"XXpathXX": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"PATH": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = None
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = None
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get(None, "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", None)
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", )
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("XXlinkXX", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("LINK", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "XXXX")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = None
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get(None, {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", None)
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get({})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", )
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("XXmetadataXX", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("METADATA", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "XXsuccessXX": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "SUCCESS": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": False,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "XXpathXX": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "PATH": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "XXlinkXX": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "LINK": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "XXmetadataXX": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "METADATA": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"XXsuccessXX": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"SUCCESS": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": True, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "XXerrorXX": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "ERROR": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "XXRespuesta inesperada de Dropbox APIXX"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "respuesta inesperada de dropbox api"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "RESPUESTA INESPERADA DE DROPBOX API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁDropboxConnectorǁ_get_temporary_link__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera un enlace temporal para descargar un archivo.

        Args:
            params: Debe contener 'path'
        """
        path = params.get("path", "")
        if not path:
            return {"success": False, "error": "Parametro requerido: path"}

        self._log_operation("get_temporary_link", f"path={path}")

        if not self._http:
            return {"success": False, "error": "HttpClient no inicializado. Llame connect() primero."}

        try:
            resp = self._http.post(
                "/files/get_temporary_link",
                json={"path": path},
            )

            if resp.ok:
                body = resp.json() if isinstance(resp.body, str) else resp.body
                if isinstance(body, dict):
                    link = body.get("link", "")
                    metadata = body.get("metadata", {})
                    return {
                        "success": True,
                        "path": path,
                        "link": link,
                        "metadata": metadata,
                    }
                return {"success": False, "error": "Respuesta inesperada de Dropbox API"}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

mutants_xǁDropboxConnectorǁ__init____mutmut['_mutmut_orig'] = DropboxConnector.xǁDropboxConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ__init____mutmut['xǁDropboxConnectorǁ__init____mutmut_1'] = DropboxConnector.xǁDropboxConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ__init____mutmut['xǁDropboxConnectorǁ__init____mutmut_2'] = DropboxConnector.xǁDropboxConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ__init____mutmut['xǁDropboxConnectorǁ__init____mutmut_3'] = DropboxConnector.xǁDropboxConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ__init____mutmut['xǁDropboxConnectorǁ__init____mutmut_4'] = DropboxConnector.xǁDropboxConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ__init____mutmut['xǁDropboxConnectorǁ__init____mutmut_5'] = DropboxConnector.xǁDropboxConnectorǁ__init____mutmut_5 # type: ignore # mutmut generated

mutants_xǁDropboxConnectorǁconnect__mutmut['_mutmut_orig'] = DropboxConnector.xǁDropboxConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁconnect__mutmut['xǁDropboxConnectorǁconnect__mutmut
] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_44 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_45'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_45 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_46'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_46 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_47'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_47 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_48'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_48 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_49'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_49 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_50'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_50 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_51'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_51 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_52'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_52 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_53'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_53 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_54'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_54 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_55'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_55 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_56'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_56 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_57'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_57 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_58'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_58 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_59'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_59 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_60'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_60 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_61'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_61 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_62'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_62 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_63'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_63 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_64'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_64 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_65'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_65 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_66'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_66 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_67'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_67 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_68'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_68 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_69'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_69 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_70'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_70 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_71'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_71 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_72'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_72 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_73'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_73 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_74'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_74 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_75'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_75 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_76'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_76 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_77'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_77 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_78'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_78 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_79'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_79 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_80'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_80 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_81'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_81 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_82'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_82 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_83'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_83 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_84'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_84 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_85'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_85 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_86'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_86 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_87'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_87 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_88'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_88 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_89'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_89 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_90'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_90 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_91'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_91 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_create_folder__mutmut['xǁDropboxConnectorǁ_create_folder__mutmut_92'] = DropboxConnector.xǁDropboxConnectorǁ_create_folder__mutmut_92 # type: ignore # mutmut generated

mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['_mutmut_orig'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_1'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_2'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_3'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_4'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_5'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_6'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_7'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_8'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_9'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_10'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_11'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_12'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_13'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_14'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_15'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_16'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_17'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_18'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_19'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_20'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_21'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_22'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_23'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_24'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_25'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_26'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_27'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_28'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_29'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_30'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_31'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_32'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_33'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_34'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_35'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_35 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_36'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_36 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_37'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_37 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_38'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_38 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_39'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_39 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_40'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_40 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_41'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_41 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_42'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_42 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_43'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_43 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_44'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_44 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_45'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_45 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_46'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_46 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_47'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_47 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_48'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_48 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_49'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_49 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_50'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_50 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_51'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_51 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_52'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_52 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_53'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_53 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_54'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_54 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_55'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_55 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_56'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_56 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_57'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_57 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_58'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_58 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_59'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_59 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_60'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_60 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_61'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_61 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_62'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_62 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_63'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_63 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_64'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_64 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_65'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_65 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_66'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_66 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_67'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_67 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_68'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_68 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_69'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_69 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_70'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_70 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_71'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_71 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_72'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_72 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_73'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_73 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_74'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_74 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_75'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_75 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_76'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_76 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_77'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_77 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_78'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_78 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_79'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_79 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_80'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_80 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_81'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_81 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_82'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_82 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_83'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_83 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_84'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_84 # type: ignore # mutmut generated
mutants_xǁDropboxConnectorǁ_get_temporary_link__mutmut['xǁDropboxConnectorǁ_get_temporary_link__mutmut_85'] = DropboxConnector.xǁDropboxConnectorǁ_get_temporary_link__mutmut_85 # type: ignore # mutmut generated


DROPBOX_SCHEMA = ConnectorSchema(
    name="dropbox",
    version="1.0.0",
    description="Sube, descarga y gestiona archivos en cuentas de Dropbox",
    category="cloud_storage",
    icon="folder",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="upload_file", description="Sube un archivo", category="write"),
        ActionDefinition(name="download_file", description="Descarga un archivo", category="read"),
        ActionDefinition(name="list_folder", description="Lista contenido de carpeta", category="read"),
        ActionDefinition(name="delete_file", description="Elimina un archivo", category="delete"),
        ActionDefinition(name="create_folder", description="Crea una carpeta", category="write"),
        ActionDefinition(name="get_temporary_link", description="Genera enlace temporal", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="oauth2", required_fields=["access_token"], description="Dropbox OAuth2 Access Token")
    ],
)
